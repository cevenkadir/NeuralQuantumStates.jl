```@meta
CurrentModule = LatticeSpaceGroups
```

# Lattice types

The [`Lattice`](@ref) type, the [`LatticeBasis`](@ref) describing its unit cell, and the
accessors for its sites and bonds. See [Lattices](@ref) in the manual for how to use them.

## Index

```@index
Pages = ["lattices.md"]
```

```@autodocs
Modules = [LatticeSpaceGroups]
Pages = ["lattice.jl"]
```
