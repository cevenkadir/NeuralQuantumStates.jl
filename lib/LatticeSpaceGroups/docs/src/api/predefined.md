```@meta
CurrentModule = LatticeSpaceGroups
```

# Lattice specifications

The lattice specs and [`build`](@ref), which turns one into a [`Lattice`](@ref). A spec
validates its arguments on construction, so an invalid lattice is rejected where it is described
rather than deep inside the build.

## Index

```@index
Pages = ["predefined.md"]
```

```@autodocs
Modules = [LatticeSpaceGroups]
Pages = ["predefined_lattices.jl"]
```
