```@meta
CurrentModule = ConnectedBasisConfigurations
```

# Configurations and local operators

Converting between packed integers and physical local values, and the single-site matrices that
agree with SymBasis's digit ordering. See [Local operators and configurations](@ref) in the
manual for the two conventions these exist to keep straight.

## Index

```@index
Pages = ["configurations.md"]
```

```@autodocs
Modules = [ConnectedBasisConfigurations]
Pages = ["unpack.jl", "local_operators.jl"]
```
