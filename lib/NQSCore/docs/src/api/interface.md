```@meta
CurrentModule = NQSCore
```

# Interfaces

The abstract types and the verbs that act on them. Adding methods to these is how an ansatz, a sampler or a preconditioner plugs into the stack — see [Extending the interfaces](@ref) for what each contract requires.

## Index

```@index
Pages = ["interface.md"]
```

```@autodocs
Modules = [NQSCore]
Pages = ["interface.jl"]
```
