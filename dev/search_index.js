var documenterSearchIndex = {"docs":
[{"category":"section","location":"basics/#Basics","page":"Basics","text":"Installing NeuralQuantumStates gives you the whole stack; one using reaches all of it.\n\nusing NeuralQuantumStates","title":"Basics"},{"category":"section","location":"basics/#A-lattice","page":"Basics","text":"Lattices are described by a specification — a struct saying what you want — which build turns into a lattice. Here is a periodic chain of 8 sites:\n\nlat = build(Hypercube([8]; periodic=true))\n\nIts bonds are pairs of site indices, and those indices are what operators act on:\n\nbonds(lat)","title":"A lattice"},{"category":"section","location":"basics/#A-model","page":"Basics","text":"Models work the same way: a specification, then build.\n\nmodel = build(TransverseFieldIsing(lat; J=1.0, h_x=1.0, h_z=1.0))\n\nA Model pairs the Hamiltonian with the space it acts on — the Hamiltonian alone knows which sites it touches, but not that they are spin-1/2:\n\nmodel.dof, model.nsites\n\nThe Hamiltonian is an ordinary OperatorAlgebra OpSum, so everything that package offers applies to it — sparse and dense conversion, commutators, ITensor export:\n\ntypeof(model.hamiltonian)","title":"A model"},{"category":"section","location":"basics/#The-computational-basis","page":"Basics","text":"b = basis(model)\nlength(b.states)\n\nStates are packed integers rather than vectors, which is what makes symmetry-reduced bases practical. configurations unpacks one into physical local values:\n\nconfigurations(model.dof, b.states[6], model.nsites)","title":"The computational basis"},{"category":"section","location":"basics/#Connected-configurations","page":"Basics","text":"The kernel of variational Monte Carlo: for a configuration vert s rangle, which vert s rangle does the Hamiltonian connect it to, and with what matrix element langle s vert hatH vert s rangle?\n\nres = connected_padded(model.hamiltonian, b.states[1:3])\nres.mels\n\nColumns are padded to a common height with a zero matrix element and a repeat of the sample itself, so the local energy\n\nE_loc(s) = sum(res.mels[:, i] .* exp.(logψ.(res.configs[:, i]) .- logψ(s)))\n\nneeds no masking — a zero contributes nothing, and every padded row is still a configuration the wavefunction can be evaluated on. res.counts records how many entries of each column are real rather than padding.\n\nPassing the Hamiltonian directly, as above, flattens it on every call. Compile it once when it is going to be used more than once, which in an optimization loop it always is:\n\ncompiled = compile(model.hamiltonian)\nconnected_padded(compiled, b.states[1:3]).counts\n\nThe batch axis follows the input array, so a (steps, chains) block of Monte Carlo samples goes in without reshaping. See the ConnectedConfigs manual for the full contract.","title":"Connected configurations"},{"category":"section","location":"basics/#Symmetry-sectors","page":"Basics","text":"A lattice knows its own symmetries, and can hand SymBasis the site permutations it needs:\n\ndofo = dof_object(model.dof)\nsector = basis(model, sym(Translational(0, lat), dofo))\nlength(sector.states)\n\nSumming the dimensions of every momentum sector recovers the full space:\n\nsum(length(basis(model, sym(Translational(k, lat), dofo)).states) for k in 0:7)\n\nconnected_padded takes such a basis directly, folding each connected configuration back onto its orbit representative with the right norm and phase factors:\n\nconnected_padded(model.hamiltonian, sector.states[1:3], sector).mels","title":"Symmetry sectors"},{"category":"section","location":"basics/#Optimizing","page":"Basics","text":"Everything above is setup; this is the point of it. A VMC driver ties a variational state, a Hamiltonian, a preconditioner, and an optimizer together.\n\nusing DifferentiationInterface, ForwardDiff, Random\n\nnsites = 6\nlat6 = build(Hypercube([nsites]; periodic=true))\nmodel6 = build(TransverseFieldIsing(lat6; J=1.0, h_x=1.0))\nb6 = basis(model6)\n\n# The exact ansatz: one parameter per basis state. Useless for anything large, indispensable\n# for checking that the machinery is right, since it can represent any state.\nansatz = LogStateVector(model6.dof, nsites, b6)\nstate = FullSumState(ansatz, init_parameters(ansatz, Xoshiro(0); scale=0.1), AutoForwardDiff())\n\ndriver = VMC(state, model6.hamiltonian;\n    preconditioner=StochasticReconfiguration(; diag_shift=1e-2),\n    optimizer=Descent(0.05))\n\nlog = run!(driver; iterations=2000, callbacks=(InvalidLossStopping(),))\nfinal_energy(log)\n\nCompare against exact diagonalization:\n\nusing LinearAlgebra\nH6 = zeros(ComplexF64, length(b6.states), length(b6.states))\nlet index = Dict(s => i for (i, s) in pairs(b6.states)),\n    res = connected_padded(model6.hamiltonian, b6.states)\n    for n in eachindex(b6.states), j in 1:res.counts[n]\n        H6[index[res.configs[j, n]], n] += res.mels[j, n]\n    end\nend\nminimum(real(eigvals(Hermitian(H6))))\n\nwarning: Stochastic reconfiguration wants a smaller step\nThe preconditioned update is routinely one to two orders of magnitude larger than the raw gradient. A learning rate that works for plain gradient descent will overshoot, leaving the run plateaued above the ground state with a large energy variance. Start near 0.05 with diag_shift = 0.01.","title":"Optimizing"},{"category":"section","location":"basics/#A-neural-ansatz","page":"Basics","text":"For anything beyond a toy the ansatz has to compress. An RBM has a number of parameters linear in the system size rather than exponential:\n\nrbm = LuxAnsatz(RBM(nsites, 2), model6.dof, nsites; rng=Xoshiro(1))\nNQSCore.n_parameters(rbm), length(b6.states)","title":"A neural ansatz"},{"category":"section","location":"basics/#Sampling-instead-of-summing","page":"Basics","text":"FullSumState enumerates the whole Hilbert space, so it is limited to small systems. Swapping in an MCState changes nothing else about the code:\n\nstarts = random_configurations(model6.dof, nsites, 8, Xoshiro(2))\nsampler = MetropolisSampler(LocalRule(), starts;\n    n_chains=8, n_samples=5_000, burn_in=1_000)\nmc = MCState(ansatz, parameters(state), sampler; backend=AutoForwardDiff(), rng=Xoshiro(3))\nexpect(mc, model6.hamiltonian)\n\nThe reported error bar is corrected for autocorrelation, and r_hat compares the chains against one another — anything much above 1.01 means the error bar cannot be trusted.","title":"Sampling instead of summing"},{"category":"section","location":"lib/nqscore/#NQSCore","page":"NQSCore","text":"See NQSCore.jl in the manual for what this package is for and how to use it.","title":"NQSCore"},{"category":"section","location":"lib/nqscore/#Index","page":"NQSCore","text":"Pages = [\"nqscore.md\"]","title":"Index"},{"category":"module","location":"lib/nqscore/#NQSCore.NQSCore","page":"NQSCore","text":"NQSCore\n\nThe interface backbone of the neural-quantum-states stack, plus the two variational state types and the log-derivative machinery they share.\n\nThis corresponds to NetKet's netket/vqs together with netket/stats. It exists so that NQSAnsatze, NQSSamplers, and NQSOptimisers can be written against one agreed set of abstract types and function names without ever depending on each other — the role SciMLBase plays for SciML, or ChainRulesCore for the autodiff ecosystem.\n\nWhat lives here\n\nInterfaces: AbstractAnsatz, AbstractVariationalState, AbstractSampler, AbstractPreconditioner, and the verbs log_amplitude, local_energy, log_derivatives, parameters, setparameters!, expect, expect_and_grad, sample.\nTwo state types, mirroring NetKet's vqs/mc/ and vqs/full_summ/: MCState and FullSumState. Both satisfy the same expect interface, so a model can be validated by exact summation before any Markov chain is involved.\nStats: the single return type of every expect — mean, error of the mean, variance, integrated autocorrelation time, and split-R̂.\nlog_derivatives: the O_k = ∂ log ψ(s) / ∂θ_k matrix, including the complex arithmetic that variational Monte Carlo needs and that is easy to get subtly wrong.\nReference implementations with no approximations in them — LogStateVector and ExactSampler — so that the machinery can be tested against exact answers rather than against itself.\n\nBackends\n\nAutomatic differentiation is reached through DifferentiationInterface.jl, and every concrete backend (Enzyme, Zygote, Reactant) as well as Lux itself is a weak dependency loaded through a package extension. using NQSCore on its own pulls in no autodiff and no GPU code.\n\nExample\n\nusing NQSCore, ConnectedConfigs, SymBasis, DifferentiationInterface, ForwardDiff\n\ndof, nsites = Spin(1 // 2), 4\nb = basis(dof_object(dof), nsites)\n\na = LogStateVector(dof, nsites, b)\nvs = FullSumState(a, init_parameters(a); backend=AutoForwardDiff())\n\nexpect(vs, H)                      # exact, zero error bar\nE, ∇ = expect_and_grad(vs, H)      # ...and its gradient\n\n\n\n\n\n","title":"NQSCore.NQSCore"},{"category":"type","location":"lib/nqscore/#NQSCore.AbstractAnsatz","page":"NQSCore","text":"AbstractAnsatz\n\nA parametrized wavefunction: something that maps a batch of configurations to log-amplitudes.\n\nThe single method every ansatz must provide is log_amplitude. Keeping the interface this narrow is deliberate — it is what lets NQSAnsatze build Lux models, NQSSamplers draw from |ψ|², and NQSOptimisers precondition gradients without any of them depending on each other.\n\nParameters are not stored in the ansatz. An ansatz describes the functional form; the parameters live in the AbstractVariationalState that owns it. That separation is what makes it possible to evaluate the same ansatz at perturbed parameters — which is exactly what computing a Jacobian does.\n\n\n\n\n\n","title":"NQSCore.AbstractAnsatz"},{"category":"type","location":"lib/nqscore/#NQSCore.AbstractPreconditioner","page":"NQSCore","text":"AbstractPreconditioner\n\nA transformation applied to the raw gradient before it is handed to an optimizer — stochastic reconfiguration, its kernel-trick variant, or plain identity.\n\nNQSOptimisers provides the implementations; the type lives here so that a driver can accept one without depending on that package.\n\n\n\n\n\n","title":"NQSCore.AbstractPreconditioner"},{"category":"type","location":"lib/nqscore/#NQSCore.AbstractSampler","page":"NQSCore","text":"AbstractSampler\n\nA way of drawing configurations distributed according to |ψ|².\n\nThe interface is sample. NQSSamplers provides Metropolis and autoregressive samplers; the only one here is ExactSampler, which enumerates the basis and is meant for testing rather than for production use.\n\n\n\n\n\n","title":"NQSCore.AbstractSampler"},{"category":"type","location":"lib/nqscore/#NQSCore.AbstractVariationalState","page":"NQSCore","text":"AbstractVariationalState\n\nA variational wavefunction: an AbstractAnsatz, a set of parameters, and a way of estimating expectation values.\n\nTwo implementations ship here, and they differ only in how they average:\n\nFullSumState sums exactly over the whole basis. No sampling, no error bars, no Markov chain — feasible only for small systems, and indispensable for testing, since any disagreement with it is a bug rather than noise.\nMCState estimates the same quantities by Monte Carlo.\n\nBoth satisfy the same interface, so a model can be developed against exact summation and then scaled up by swapping the state type and changing nothing else.\n\nRequired methods\n\nparameters, setparameters!, ansatz, expect, expect_and_grad, and samples.\n\n\n\n\n\n","title":"NQSCore.AbstractVariationalState"},{"category":"type","location":"lib/nqscore/#NQSCore.ExactSampler","page":"NQSCore","text":"ExactSampler(basis, n_samples) <: AbstractSampler\n\nDraws independent samples from the exact distribution |ψ(s)|², by enumerating the basis and computing every probability.\n\nThe counterpart of NetKet's sampler.ExactSampler, and it lives here alongside FullSumState for the same reason: it is the reference implementation, not a production tool. Enumerating the basis costs the dimension of the Hilbert space, so this is only for small systems — but its samples are genuinely independent, with no burn-in, no autocorrelation, and no proposal to tune.\n\nThat makes it the right instrument for a specific job: it isolates Monte Carlo noise from Markov chain pathology. An MCState driven by this sampler must converge to the matching FullSumState, and if it does not, the problem is in the estimator rather than in mixing. NQSSamplers provides the Metropolis and autoregressive samplers meant for real use.\n\nFields\n\nbasis: the basis to enumerate.\nn_samples::Int: how many independent samples to draw.\n\n\n\n\n\n","title":"NQSCore.ExactSampler"},{"category":"type","location":"lib/nqscore/#NQSCore.FullSumState","page":"NQSCore","text":"FullSumState(ansatz, parameters; backend) <: AbstractVariationalState\n\nA variational state that sums exactly over the whole basis instead of sampling.\n\nThe counterpart of NetKet's FullSumState, and the reason it matters is testing: it satisfies the same interface as MCState but has no sampling noise at all, so a disagreement between the two is a bug rather than a fluctuation. Any model can be developed and debugged against exact summation and then scaled up by swapping the state type.\n\nCost is the dimension of the Hilbert space, so this is for small systems only.\n\nFields\n\nansatz, parameters: the wavefunction.\nbackend: the DifferentiationInterface backend used for log_derivatives.\n\n\n\n\n\n","title":"NQSCore.FullSumState"},{"category":"method","location":"lib/nqscore/#NQSCore.FullSumState-Tuple{AbstractAnsatz, Any, Any}","page":"NQSCore","text":"FullSumState(ansatz, parameters, backend; basis=nothing)\n\nThe basis is what the state sums over, so it belongs to the state rather than to the ansatz — an ansatz is a functional form and has no opinion about which configurations exist. It defaults to the full space implied by the ansatz's dof and nsites; pass basis explicitly to sum over a symmetry sector instead.\n\n\n\n\n\n","title":"NQSCore.FullSumState"},{"category":"type","location":"lib/nqscore/#NQSCore.LogStateVector","page":"NQSCore","text":"LogStateVector(dof, nsites, basis) <: AbstractAnsatz\n\nThe exact ansatz: one complex parameter per basis state, holding that state's log-amplitude directly.\n\nThis is NetKet's models.LogStateVector, and it exists for the same reason. It has as many parameters as the Hilbert space has dimensions, so it is useless for anything large — but it can represent any state exactly, which makes it the reference every approximate ansatz is measured against. Optimizing it must reproduce exact diagonalization; if it does not, the bug is in the optimization machinery rather than in the ansatz.\n\nIt is also the one ansatz whose log-derivatives are known in closed form: since log ψ(s) = θ_{i(s)}, the matrix O is a one-hot indicator of which basis state each sample is. That makes it the natural test of log_derivatives itself.\n\nFields\n\ndof: The SymBasis degree-of-freedom specification.\nnsites::Int: Number of sites.\nbasis: The basis whose states the parameters are indexed by.\n\n\n\n\n\n","title":"NQSCore.LogStateVector"},{"category":"type","location":"lib/nqscore/#NQSCore.MCState","page":"NQSCore","text":"MCState(ansatz, parameters, sampler; backend, rng) <: AbstractVariationalState\n\nA variational state that estimates expectation values by Monte Carlo.\n\nSamples are drawn once and cached; resample! draws a fresh set. Caching is what makes expect and expect_and_grad consistent with one another — both must be computed from the same samples, or the gradient does not correspond to the reported energy.\n\nFields\n\nansatz, parameters: the wavefunction.\nsampler: an AbstractSampler.\nbackend: the DifferentiationInterface backend for log_derivatives.\nrng: the random source.\n\n\n\n\n\n","title":"NQSCore.MCState"},{"category":"type","location":"lib/nqscore/#NQSCore.Stats","page":"NQSCore","text":"Stats\n\nThe result of an expect call: an estimate together with everything needed to judge how much to trust it.\n\nEvery state type returns this, so downstream code never has to branch on whether a number came from exact summation or from a Markov chain.\n\nFields\n\nmean: The estimate itself.\nerror_of_mean: Standard error, corrected for autocorrelation. Zero for an exact result.\nvariance: Variance of the sampled quantity. For an energy this is a physical quantity in its own right — it vanishes at an exact eigenstate, which makes it the sharpest available convergence diagnostic.\ntau_corr: Integrated autocorrelation time, in units of sampling steps. 1 means independent samples.\nr_hat: Split-R̂ convergence diagnostic. 1 means converged; anything above about 1.01 means the chains disagree and the error bar is not trustworthy.\n\nWhy the error bar is not just std/sqrt(n)\n\nMarkov chain samples are correlated, so the naive formula understates the error by roughly sqrt(tau_corr). Reporting that number unadjusted is the single most common way to \"converge\" to a wrong answer with confident-looking error bars, so the correction is applied here rather than left to the caller.\n\n\n\n\n\n","title":"NQSCore.Stats"},{"category":"method","location":"lib/nqscore/#NQSCore._gradient-Tuple{AbstractMatrix, AbstractVector, Union{Nothing, AbstractVector}}","page":"NQSCore","text":"_gradient(O, E_loc, weights) -> Vector\n\nThe variational energy gradient 2 Re[⟨O* E_loc⟩ - ⟨O*⟩⟨E_loc⟩].\n\nweights is nothing for an unweighted Monte Carlo average, or the exact probabilities for a full summation.\n\n\n\n\n\n","title":"NQSCore._gradient"},{"category":"method","location":"lib/nqscore/#NQSCore._log_derivatives-Tuple{Any, AbstractVector{<:Complex}, Any, Any, Any, Bool}","page":"NQSCore","text":"Complex parameters: differentiate with respect to the real and imaginary parts separately.\n\n\n\n\n\n","title":"NQSCore._log_derivatives"},{"category":"method","location":"lib/nqscore/#NQSCore._log_derivatives-Tuple{Any, AbstractVector{<:Real}, Any, Any, Any, Bool}","page":"NQSCore","text":"Real parameters: one pass over the stacked real and imaginary parts of log ψ.\n\n\n\n\n\n","title":"NQSCore._log_derivatives"},{"category":"method","location":"lib/nqscore/#NQSCore._match_parameter_shape-Tuple{AbstractVector, Any}","page":"NQSCore","text":"_match_parameter_shape(∇, θ) -> gradient\n\nPut a raw gradient into the same shape as the parameters, so that θ .- η .* ∇ is meaningful.\n\nFor complex parameters differentiated non-holomorphically, log_derivatives returns 2n columns — ∂/∂θ_re followed by ∂/∂θ_im — and the gradient inherits that length. Descending in the real parameterization means θ_re -= η g_re and θ_im -= η g_im simultaneously, which is exactly θ -= η (g_re + i g_im). Recombining here rather than at the call site keeps the promise that the gradient always matches the parameters, whatever the parameterization: an optimizer should never have to ask how the ansatz was parameterized.\n\n\n\n\n\n","title":"NQSCore._match_parameter_shape"},{"category":"function","location":"lib/nqscore/#NQSCore.ansatz","page":"NQSCore","text":"ansatz(state) -> AbstractAnsatz\n\nThe ansatz state is built on.\n\n\n\n\n\n","title":"NQSCore.ansatz"},{"category":"function","location":"lib/nqscore/#NQSCore.centered","page":"NQSCore","text":"centered(O, weights=nothing) -> Matrix\n\nSubtract the (optionally weighted) mean of each column of O.\n\nEvery use of O in variational Monte Carlo wants the centered version: the energy gradient is a covariance, and the quantum geometric tensor is a covariance matrix. Centering also removes the direction corresponding to a global change of normalization, which carries no physical information and would otherwise make the geometric tensor singular.\n\n\n\n\n\n","title":"NQSCore.centered"},{"category":"method","location":"lib/nqscore/#NQSCore.default_basis-Tuple{AbstractAnsatz}","page":"NQSCore","text":"default_basis(ansatz) -> SymBasis.Basis\n\nThe full computational basis implied by an ansatz's degrees of freedom.\n\n\n\n\n\n","title":"NQSCore.default_basis"},{"category":"method","location":"lib/nqscore/#NQSCore.default_basis-Tuple{LogStateVector}","page":"NQSCore","text":"A LogStateVector is indexed by a specific basis, so that is the one to sum over.\n\n\n\n\n\n","title":"NQSCore.default_basis"},{"category":"function","location":"lib/nqscore/#NQSCore.exact_stats","page":"NQSCore","text":"exact_stats(value, variance=0.0) -> Stats\n\nA Stats for a quantity computed exactly: zero error, no autocorrelation, converged by construction.\n\n\n\n\n\n","title":"NQSCore.exact_stats"},{"category":"function","location":"lib/nqscore/#NQSCore.expect","page":"NQSCore","text":"expect(state, operator) -> Stats\n\nExpectation value ⟨ψ|Ô|ψ⟩ / ⟨ψ|ψ⟩, with an uncertainty estimate.\n\nAlways returns a Stats, whichever state type produced it. A FullSumState reports a zero error bar because its answer is exact; an MCState reports the standard error of the mean along with the autocorrelation diagnostics needed to judge whether that error bar can be believed.\n\n\n\n\n\n","title":"NQSCore.expect"},{"category":"function","location":"lib/nqscore/#NQSCore.expect_and_grad","page":"NQSCore","text":"expect_and_grad(state, operator) -> (Stats, gradient)\n\nexpect together with the gradient of that expectation value with respect to the variational parameters.\n\nThe gradient is the usual variational-Monte-Carlo estimator\n\npartial_k langle E rangle = 2  mathrmRe\n    left langle O_k^* E_mathrmloc rangle\n          - langle O_k^* rangle langle E_mathrmloc rangle right\n\nwith O_k the log-derivatives from log_derivatives. The subtraction of ⟨O_k^*⟩⟨E_loc⟩ is not cosmetic: without it the estimator has a non-vanishing variance even at an exact eigenstate.\n\n\n\n\n\n","title":"NQSCore.expect_and_grad"},{"category":"method","location":"lib/nqscore/#NQSCore.flatten_parameters-Tuple{AbstractVector}","page":"NQSCore","text":"flatten_parameters(θ) -> (flat, restore)\n\nFlatten a parameter container to a plain vector, and return the function that rebuilds it.\n\nLux hands back parameters as nested NamedTuples, while every piece of linear algebra downstream — the quantum geometric tensor, the gradient, the optimizer step — wants a flat vector. ComponentArrays provides the bijection; restore is what carries an updated flat vector back into the nested shape the ansatz expects.\n\n\n\n\n\n","title":"NQSCore.flatten_parameters"},{"category":"function","location":"lib/nqscore/#NQSCore.init_parameters","page":"NQSCore","text":"init_parameters(ansatz, rng; scale=0.01) -> Vector\n\nRandom initial parameters for ansatz.\n\nSmall values keep the initial state close to uniform, which is a deliberately uninformative starting point rather than an accidental one.\n\n\n\n\n\n","title":"NQSCore.init_parameters"},{"category":"method","location":"lib/nqscore/#NQSCore.integrated_autocorrelation-Tuple{AbstractVector{<:Real}}","page":"NQSCore","text":"integrated_autocorrelation(chain) -> Float64\n\nIntegrated autocorrelation time of a single chain, by the initial-positive-sequence estimator.\n\nThe autocorrelation function is summed until it first goes non-positive, which is the standard way of truncating a sum whose tail is pure noise. Summing the whole thing instead adds variance without adding information, and can even produce a negative estimate.\n\n\n\n\n\n","title":"NQSCore.integrated_autocorrelation"},{"category":"function","location":"lib/nqscore/#NQSCore.local_energy","page":"NQSCore","text":"local_energy(state, operator[, states]) -> AbstractVector\n\nLocal energies E_loc(s) = Σ_{s'} ⟨s|Ô|s'⟩ ψ(s')/ψ(s) for the given configurations, defaulting to the state's current samples.\n\nThis is where the variational method earns its keep: the sum runs only over configurations the operator actually connects to s, which for a local Hamiltonian is a handful, not the whole Hilbert space.\n\n\n\n\n\n","title":"NQSCore.local_energy"},{"category":"method","location":"lib/nqscore/#NQSCore.local_energy-Tuple{AbstractVariationalState, Any, AbstractArray}","page":"NQSCore","text":"local_energy(state, operator, packed_states) -> AbstractArray\n\nLocal energies E_loc(s) = Σ_{s'} ⟨s|Ô|s'⟩ ψ(s')/ψ(s).\n\nThe ratio is evaluated as exp(log ψ(s') - log ψ(s)), never as a quotient of amplitudes. That is not a micro-optimization: amplitudes underflow to zero for any system worth studying, while the difference of their logarithms stays perfectly well behaved.\n\nMulti-chain samples arrive as a (steps, chains) array, and the result keeps that shape, so the chain structure survives into statistics — which needs it for split-R̂ and for a between-chain error bar. Flattening happens here rather than in the sampler, leaving the chain layout the sampler's business.\n\n\n\n\n\n","title":"NQSCore.local_energy"},{"category":"method","location":"lib/nqscore/#NQSCore.local_estimators-Tuple{AbstractVariationalState, Any}","page":"NQSCore","text":"local_estimators(state, operator; holomorphic=false) -> (; E, O, weights)\n\nEverything an estimator built from this state needs, computed once from one set of samples.\n\nE: local energies, flattened.\nO: the log-derivative matrix, (n_samples, n_parameters).\nweights: exact Born probabilities for a FullSumState, nothing for an MCState whose samples are already distributed according to |ψ|².\n\nThe plain energy gradient and the quantum geometric tensor are built from exactly these three things, so exposing them keeps NQSOptimisers from having to recompute — and, more importantly, guarantees that a preconditioned update and the energy it is derived from come from the same samples. Recomputing would silently mix two sample sets, which shows up as an optimizer that mysteriously fails to descend.\n\n\n\n\n\n","title":"NQSCore.local_estimators"},{"category":"function","location":"lib/nqscore/#NQSCore.log_amplitude","page":"NQSCore","text":"log_amplitude(ansatz, parameters, x) -> AbstractVector\n\nLog-amplitudes log ψ(x) for a batch of configurations.\n\nx is a (nsites, batch) array of physical local values — magnetic quantum numbers for a spin, occupation numbers for a boson — as produced by ConnectedConfigs.configurations. The result is a length-batch vector, generally complex: the real part carries the log-modulus and the imaginary part the phase.\n\nReturning the logarithm rather than the amplitude is what makes this numerically usable at all: amplitudes underflow catastrophically for any interesting system size, whereas the differences of logarithms that local energies actually need stay bounded.\n\n\n\n\n\n","title":"NQSCore.log_amplitude"},{"category":"method","location":"lib/nqscore/#NQSCore.log_amplitudes-Tuple{AbstractVariationalState, AbstractArray}","page":"NQSCore","text":"log_amplitudes(state, packed_states) -> Vector\n\nLog-amplitudes of state's ansatz on a vector of packed configurations, unpacking them into the numeric array the ansatz interface expects.\n\nThis is the boundary between the two representations: packed integers are what the operator kernel and the samplers work in, numeric arrays are what an ansatz consumes.\n\n\n\n\n\n","title":"NQSCore.log_amplitudes"},{"category":"method","location":"lib/nqscore/#NQSCore.log_derivatives-Tuple{AbstractAnsatz, Any, AbstractMatrix}","page":"NQSCore","text":"log_derivatives(ansatz, parameters, x; backend, holomorphic=false) -> Matrix\n\nThe log-derivative matrix O[s, k] = ∂ log ψ(x_s) / ∂θ_k, of size (batch, n_parameters).\n\nO is the central object of variational Monte Carlo beyond plain gradient descent: the energy gradient is a covariance between O and the local energies, and the quantum geometric tensor that stochastic reconfiguration inverts is S = ⟨O†O⟩ - ⟨O⟩†⟨O⟩.\n\nRows are samples and columns are parameters, matching NetKet, so that S = O'O has the shape one expects without transposing.\n\nComplex arithmetic\n\nTwo independent things can be complex here, and conflating them is the usual source of wrong gradients.\n\nA complex log-amplitude with real parameters — the common case for a neural network that outputs a modulus and a phase. O is then complex even though θ is real, and both parts come from a single automatic-differentiation pass over [real(log ψ); imag(log ψ)]. Doing it in one pass rather than two halves the cost, since the two share all of their intermediate work.\n\nComplex parameters are ambiguous until a convention is fixed:\n\nholomorphic=true assumes ψ is holomorphic in θ, so ∂/∂θ = ∂/∂θ_re, and O keeps one column per parameter.\nholomorphic=false (the default) treats real and imaginary parts as 2n independent real parameters, returning [∂/∂θ_re  ∂/∂θ_im] with 2n columns. This is always valid; the holomorphic form is a shortcut that is silently wrong when the ansatz does not satisfy it, so it must be asked for explicitly.\n\nBackend\n\nDifferentiation goes through DifferentiationInterface.jl, so backend is any of its objects — AutoForwardDiff(), AutoZygote(), AutoEnzyme(). None of those packages is a dependency here; load the one you want and pass it.\n\n\n\n\n\n","title":"NQSCore.log_derivatives"},{"category":"method","location":"lib/nqscore/#NQSCore.n_parameters-Tuple{LogStateVector}","page":"NQSCore","text":"Number of parameters of a LogStateVector: the dimension of its basis.\n\n\n\n\n\n","title":"NQSCore.n_parameters"},{"category":"function","location":"lib/nqscore/#NQSCore.parameters","page":"NQSCore","text":"parameters(state)\n\nThe variational parameters of state.\n\n\n\n\n\n","title":"NQSCore.parameters"},{"category":"method","location":"lib/nqscore/#NQSCore.probabilities-Tuple{FullSumState}","page":"NQSCore","text":"probabilities(state) -> Vector{Float64}\n\nThe exact Born probabilities |ψ(s)|² / Σ|ψ|² over the whole basis.\n\nComputed by shifting the log-amplitudes by their maximum before exponentiating, so that a wavefunction with a large log-modulus does not overflow on the way to a perfectly ordinary probability.\n\n\n\n\n\n","title":"NQSCore.probabilities"},{"category":"method","location":"lib/nqscore/#NQSCore.resample!-Tuple{MCState}","page":"NQSCore","text":"resample!(state) -> state\n\nDraw a fresh set of Monte Carlo samples.\n\n\n\n\n\n","title":"NQSCore.resample!"},{"category":"function","location":"lib/nqscore/#NQSCore.sample","page":"NQSCore","text":"sample(sampler, ansatz, parameters, rng) -> AbstractVector\n\nDraw configurations distributed according to |ψ|², returned as packed states.\n\n\n\n\n\n","title":"NQSCore.sample"},{"category":"function","location":"lib/nqscore/#NQSCore.sample_weights","page":"NQSCore","text":"sample_weights(state) -> Union{Nothing,Vector}\n\nProbability weights attached to a state's samples, or nothing when they are already drawn from |ψ|² and so carry equal weight.\n\n\n\n\n\n","title":"NQSCore.sample_weights"},{"category":"function","location":"lib/nqscore/#NQSCore.samples","page":"NQSCore","text":"samples(state) -> AbstractVector\n\nThe configurations state currently estimates expectation values from, as packed states.\n\nFor an MCState these are the drawn Monte Carlo samples; for a FullSumState they are every state in the basis.\n\n\n\n\n\n","title":"NQSCore.samples"},{"category":"function","location":"lib/nqscore/#NQSCore.setparameters!","page":"NQSCore","text":"setparameters!(state, θ) -> state\n\nReplace the variational parameters of state, invalidating anything cached from the old ones.\n\n\n\n\n\n","title":"NQSCore.setparameters!"},{"category":"method","location":"lib/nqscore/#NQSCore.setparameters!-Tuple{MCState, Any}","page":"NQSCore","text":"Changing the parameters invalidates the cached samples: they came from the old |ψ|².\n\n\n\n\n\n","title":"NQSCore.setparameters!"},{"category":"method","location":"lib/nqscore/#NQSCore.split_rhat-Tuple{AbstractMatrix{<:Real}}","page":"NQSCore","text":"split_rhat(chains) -> Float64\n\nSplit-R̂ over a matrix of chains, one chain per column.\n\nEach chain is split in half before the comparison, so that a single chain that has drifted — mean moving steadily through the run — is caught as disagreement between its own halves. R̂ computed without splitting is blind to exactly that failure.\n\n\n\n\n\n","title":"NQSCore.split_rhat"},{"category":"method","location":"lib/nqscore/#NQSCore.statistics-Tuple{AbstractMatrix}","page":"NQSCore","text":"statistics(values; n_chains=1) -> Stats\n\nReduce raw samples to a Stats.\n\nvalues may be a vector (one chain) or a (steps, chains) matrix. With more than one chain, the error of the mean is estimated from the spread between chain means, which needs no autocorrelation model at all; with a single chain it falls back to the autocorrelation-corrected standard error.\n\n\n\n\n\n","title":"NQSCore.statistics"},{"category":"method","location":"lib/nqscore/#NQSCore.weighted_statistics-Tuple{AbstractVector, AbstractVector{<:Real}}","page":"NQSCore","text":"weighted_statistics(values, weights) -> Stats\n\nExact expectation of values under the normalized probability weights.\n\nUsed by FullSumState, where the \"samples\" are the entire basis and the weights are |ψ(s)|². The reported variance is the physical variance of the quantity under that distribution, and the error bar is zero because nothing was sampled.\n\n\n\n\n\n","title":"NQSCore.weighted_statistics"},{"category":"section","location":"lib/public/#Public-API","page":"Public API","text":"This page documents the umbrella package itself: the predefined models and the Model type. The re-exported components have their own reference pages.","title":"Public API"},{"category":"section","location":"lib/public/#Index","page":"Public API","text":"Pages = [\"public.md\"]","title":"Index"},{"category":"module","location":"lib/public/#NeuralQuantumStates.NeuralQuantumStates","page":"Public API","text":"NeuralQuantumStates\n\nUmbrella package for the neural-quantum-states stack.\n\nInstalling this package gives you the whole ecosystem: it re-exports each component and adds the predefined models and, in time, the optimization drivers, callbacks, and logging.\n\nThe stack\n\nTier 1 carries no machine-learning dependencies and is usable on its own for exact diagonalization:\n\nPackage Role\nSymBasis.jl States, degrees of freedom, symmetry groups, symmetry-reduced bases\nOperatorAlgebra.jl Op/OpChain/OpSum, sparse and dense conversion, fermionic sites\nLatticeSpaceGroups Lattice geometry, bonds, and the site permutations symmetry groups need\nConnectedConfigs The batched local-energy kernel\n\nPackage Role\nNQSCore Interfaces, MCState/FullSumState, log-derivatives, statistics\nNQSAnsatze Ansätze on Lux: RBM, symmetric RBM, Jastrow\nNQSSamplers Metropolis samplers and transition rules\nNQSOptimisers Stochastic reconfiguration and MinSR\n\nThis package adds the predefined models and the VMC driver on top.\n\nPredefined models\n\nTransverseFieldIsing and ExtendedBoseHubbard are model specifications; build turns one into a Model, pairing an OpSum Hamiltonian with the space it acts on.\n\nusing NeuralQuantumStates\n\nlat = build(Hypercube([8]; periodic=true))\nmodel = build(TransverseFieldIsing(lat; J=1.0, h_x=1.0))\n\nstates = basis(model).states\nres = connected_padded(compile(model.hamiltonian), states)\n\nMigrating from the pre-split package\n\nLattices, Hilberts, and Operators are gone. Their replacements:\n\nWas Now\nLattices.build(:Hypercube, [8], 1.0; periodic=[true]) build(Hypercube([8]; periodic=true))\nHilberts.build(:Spin, 1//2, N) basis(dof_object(Spin(1//2)), N)\nHilberts.build(:Fock, n_max, N; ∑n=n) basis(dofo, N, sym(TotalBosonicNumber(n, N), dofo))\nOperators.build(:TransverseFieldIsing, h, l; ...) build(TransverseFieldIsing(l; ...))\nOperators.connected_basis_configs(H, samples) connected_padded(H, states)\n\nThree behavioural differences are deliberate: connected configurations are padded with a zero matrix element rather than missing; the degree-of-freedom axis is always first (the old code put it last for Ising and first for Bose-Hubbard); and two terms reaching the same configuration now produce two rows rather than one summed row, which every consumer sums over anyway. See ConnectedConfigs for all three, and compile for how to stop re-flattening a Hamiltonian on every call.\n\n\n\n\n\n","title":"NeuralQuantumStates.NeuralQuantumStates"},{"category":"type","location":"lib/public/#NeuralQuantumStates.AbstractModelSpec","page":"Public API","text":"AbstractModelSpec\n\nA specification of a predefined many-body model.\n\nLike a lattice specification, a model spec describes a Hamiltonian — its lattice, its local degrees of freedom, and its couplings — without building anything. Pass one to build to get a Model:\n\nlat = build(Hypercube([8]; periodic=true))\nmodel = build(TransverseFieldIsing(lat; J=1.0, h_x=1.0))\n\nConcrete specs are TransverseFieldIsing and ExtendedBoseHubbard.\n\nThis replaces the pre-split Operators.build(:TransverseFieldIsing, hilbert, lattice; ...). The Hamiltonian is now an ordinary OperatorAlgebra OpSum rather than a bespoke type, so it composes with everything that package offers — sparse, LinearMap, commutator, ITensor conversion — none of which the old hand-written operators supported.\n\n\n\n\n\n","title":"NeuralQuantumStates.AbstractModelSpec"},{"category":"type","location":"lib/public/#NeuralQuantumStates.EarlyStopping","page":"Public API","text":"EarlyStopping(; patience=50, min_delta=1e-6)\n\nStop when the energy has not improved by more than min_delta for patience iterations.\n\nJudged on the energy mean. With a Monte Carlo state the mean fluctuates, so min_delta should be set comfortably above the error bar — otherwise noise alone will look like an improvement and the run will never stop.\n\n\n\n\n\n","title":"NeuralQuantumStates.EarlyStopping"},{"category":"type","location":"lib/public/#NeuralQuantumStates.ExtendedBoseHubbard","page":"Public API","text":"ExtendedBoseHubbard{L,T} <: AbstractModelSpec\n\nH = -J sum_langle ij rangle (b^dagger_i b_j + mathrmhc)      + fracU2 sum_i n_i (n_i - 1)      + V sum_langle ij rangle n_i n_j - mu sum_i n_i\n\nConstructor\n\nExtendedBoseHubbard(lattice, n_max; J=1.0, U=1.0, V=0.0, μ=0.0)\n\nn_max is the maximum occupation per site, which truncates the bosonic ladder.\n\nnote: Sign of the chemical potential\nmu enters with a minus sign, matching what the pre-split package computed. Its own docstring advertised a plus sign, which its code did not implement.\n\n\n\n\n\n","title":"NeuralQuantumStates.ExtendedBoseHubbard"},{"category":"type","location":"lib/public/#NeuralQuantumStates.InvalidLossStopping","page":"Public API","text":"InvalidLossStopping()\n\nStop as soon as the energy becomes NaN or Inf.\n\nWorth having on by default. A diverged run otherwise keeps going for its full iteration count, propagating NaN through every parameter, and the failure is then reported as a finished run with a meaningless answer rather than as the divergence it was.\n\n\n\n\n\n","title":"NeuralQuantumStates.InvalidLossStopping"},{"category":"type","location":"lib/public/#NeuralQuantumStates.Model","page":"Public API","text":"Model\n\nA Hamiltonian together with the space it acts on.\n\nFields\n\nhamiltonian::OpSum: The Hamiltonian, as an OperatorAlgebra operator.\ndof: The SymBasis degree-of-freedom specification (Spin, Boson, ...).\nnsites::Int: Number of sites.\nlattice: The lattice it was built on.\n\ndof and nsites are carried alongside the operator because the operator alone does not determine them: an OpSum knows which sites it touches, but not that a site is a spin-1/2 rather than a spin-1, nor that sites it happens not to act on still exist.\n\n\n\n\n\n","title":"NeuralQuantumStates.Model"},{"category":"type","location":"lib/public/#NeuralQuantumStates.TransverseFieldIsing","page":"Public API","text":"TransverseFieldIsing{L,T} <: AbstractModelSpec\n\nH = J sum_langle ij rangle sigma^z_i sigma^z_j      + h_x sum_i sigma^x_i + h_z sum_i sigma^z_i\n\nConstructor\n\nTransverseFieldIsing(lattice; J=1.0, h_x=1.0, h_z=0.0)\n\nnote: Pauli matrices, not spin operators\nThe couplings multiply sigma rather than S = sigma2, matching the pre-split package. σᶻ is diag(-1, +1): digit 0 is spin down, following SymBasis's ldof = (-1//2, 1//2). This is the negative of OperatorAlgebra's PAULI_Z constant — see ConnectedConfigs.local_operators for why that distinction matters.\n\n\n\n\n\n","title":"NeuralQuantumStates.TransverseFieldIsing"},{"category":"type","location":"lib/public/#NeuralQuantumStates.VMC","page":"Public API","text":"VMC(state, hamiltonian; preconditioner=Identity(), optimizer=Descent(0.05))\n\nA variational Monte Carlo ground-state optimization.\n\nThe counterpart of NetKet's driver.VMC. Each iteration computes the energy and its gradient, passes the gradient through preconditioner, and hands the result to optimizer.\n\nChoosing a preconditioner\n\nIdentity() is plain gradient descent. It is adequate in weakly correlated regimes and stalls in strongly ordered ones, because the gradient carries a factor of the Born probability that vanishes for exactly the configurations whose amplitude must grow. StochasticReconfiguration() divides that factor out and is the right default for anything hard; see NQSOptimisers.\n\nFields\n\nstate: an AbstractVariationalState — FullSumState for exact summation, MCState for sampling.\nhamiltonian: the operator to minimize.\npreconditioner: an AbstractPreconditioner.\noptimizer: any Optimisers.jl rule (Descent, Adam, ...).\n\n\n\n\n\n","title":"NeuralQuantumStates.VMC"},{"category":"type","location":"lib/public/#NeuralQuantumStates.VMCLog","page":"Public API","text":"VMCLog\n\nThe record of an optimization run: one Stats per iteration, plus the wall-clock time.\n\nKept in memory rather than streamed to a file. Serialization is deliberately left to the caller — writing JSON or HDF5 would mean a dependency the rest of the stack does not need, and the fields here are plain arrays that any format handles.\n\nFields\n\nenergies::Vector{Stats}: the energy at every iteration.\nelapsed::Float64: seconds taken.\n\n\n\n\n\n","title":"NeuralQuantumStates.VMCLog"},{"category":"method","location":"lib/public/#NeuralQuantumStates.final_energy-Tuple{VMCLog}","page":"Public API","text":"Final energy of a run, or nothing if it never got started.\n\n\n\n\n\n","title":"NeuralQuantumStates.final_energy"},{"category":"method","location":"lib/public/#NeuralQuantumStates.run!-Tuple{VMC}","page":"Public API","text":"run!(driver; iterations, callbacks=[], verbose=false) -> VMCLog\n\nRun the optimization.\n\nEach callback is called as callback(iteration, stats, state) after the parameters are updated; returning false stops the run. That is how EarlyStopping and InvalidLossStopping work, and it is enough for a user callback to do the same.\n\n\n\n\n\n","title":"NeuralQuantumStates.run!"},{"category":"method","location":"lib/public/#SymBasis.Bases.basis-Tuple{Model}","page":"Public API","text":"basis(model::Model, symmetry...) -> SymBasis.Basis\n\nThe computational basis of model, optionally restricted to a symmetry sector.\n\nbasis(model)                                        # full space\nbasis(model, sym(Translational(0, lat), dof_object(model.dof)))\n\n\n\n\n\n","title":"SymBasis.Bases.basis"},{"category":"section","location":"manual/latticespacegroups/#Lattices-and-symmetries","page":"Lattices and symmetries","text":"Provided by LatticeSpaceGroups, which has its own site.\n\nSymmetry-reduced exact diagonalization needs a site permutation: which site does site i become under a translation, a reflection, a rotation? Writing one by hand only ever works for a chain — mod1.((1:N) .+ 1, N) and nothing else. LatticeSpaceGroups derives them from the lattice geometry instead, so a kagome torus is no harder than a chain.\n\nIt is re-exported, so everything below is available from using NeuralQuantumStates.\n\nusing NeuralQuantumStates","title":"Lattices and symmetries"},{"category":"section","location":"manual/latticespacegroups/#Specifications,-then-build","page":"Lattices and symmetries","text":"A lattice is described by a specification — a struct saying what you want, validating its arguments where you write them rather than deep inside the construction — which build turns into a Lattice:\n\nlat = build(Kagome([2, 2], 1.0; periodic=true))\n\nThe zoo covers Hypercube (with Square and Cube shorthands), Triclinic, Triangular, Honeycomb, Kagome, BCC, FCC, Diamond and Pyrochlore. Anything else you build yourself from a LatticeBasis and, where the connectivity is not distance-derived, an explicit bond list.\n\nBonds come out as site-index pairs, listed once and sorted — ready to be the site identifiers of an operator term:\n\nbonds(build(Hypercube([4]; periodic=true)))","title":"Specifications, then build"},{"category":"section","location":"manual/latticespacegroups/#Permutations-for-a-symmetry-reduced-basis","page":"Lattices and symmetries","text":"The point of the geometry is what it produces: the permutation vectors SymBasis's symmetry constructors take.\n\ntranslation_permutation(lat, 1)\n\nSymBasis's Translational, SpatialReflection and Rotational also accept a lattice directly — a package extension that loads when both packages are present, which in this stack they always are:\n\nchain = build(Hypercube([8]; periodic=true))\ndofo = dof_object(Spin(1 // 2))\nsector = basis(dofo, 8, sym(Translational(0, chain), dofo))\nlength(sector.states)","title":"Permutations for a symmetry-reduced basis"},{"category":"section","location":"manual/latticespacegroups/#Real-space-groups,-not-idealised-ones","page":"Lattices and symmetries","text":"point_group and space_group return the operations that actually permute this finite lattice's sites under its boundary conditions, rather than the infinite-lattice answer. That includes operations whose symmetry centre is not the coordinate origin — a honeycomb's six-fold axis passes through a hexagon centre, an open chain's mirror sits at its midpoint — which is why those lattices report their full point group rather than a subgroup of it.\n\nlength(point_group(lat))     # D₆, found without being told the symmetry centre","title":"Real space groups, not idealised ones"},{"category":"section","location":"manual/latticespacegroups/#Full-documentation","page":"Lattices and symmetries","text":"LatticeSpaceGroups is registered as a package in its own right and has a complete site:\n\nLattices — the predefined zoo, bonds and neighbour shells, and building one by hand\nSymmetries — translations, point groups, space groups, and the bridge to SymBasis\nSymmetry-reduced bases\nThree-dimensional lattices\nBoundary conditions and symmetry\nAPI reference","title":"Full documentation"},{"category":"section","location":"manual/connectedconfigs/#Connected-configurations","page":"Connected configurations","text":"Provided by ConnectedConfigs, which has its own site.\n\nVariational Monte Carlo spends nearly all its time answering one question, several thousand times per optimization step: given a configuration vert s rangle and a Hamiltonian hatH, which vert s rangle does it connect to, and with what matrix element langle s vert hatH vert s rangle? That is the local energy,\n\nE_mathrmloc(s) = sum_s langle s vert hatH vert s rangle \n                      fracpsi(s)psi(s)\n\nand ConnectedConfigs answers it for a whole batch at once, returning padded arrays rather than a dictionary per sample — the Julia counterpart of NetKet's get_conn_padded.\n\nIt is re-exported, so everything below is available from using NeuralQuantumStates.\n\nusing NeuralQuantumStates\n\nlat = build(Hypercube([6]; periodic=true))\nmodel = build(TransverseFieldIsing(lat; J=1.0, h_x=1.0))\nstates = basis(model).states\nnothing # hide","title":"Connected configurations"},{"category":"section","location":"manual/connectedconfigs/#Compile-once","page":"Connected configurations","text":"compile flattens an operator into the form the kernel runs on: Jordan–Wigner strings resolved, sums distributed over products, same-site factors multiplied out, every local matrix column-compressed. Everything that does not depend on the configuration is decided here, once.\n\ncompiled = compile(model.hamiltonian)\n\nPassing the Hamiltonian straight to connected_padded works too — it just compiles on every call. Over a large batch that is amortized; over the two single-configuration queries a Hamiltonian-driven Metropolis rule makes per step, it is the runtime. Compile whenever the operator outlives the call, which in an optimization loop it always does.","title":"Compile once"},{"category":"section","location":"manual/connectedconfigs/#The-batched-call","page":"Connected configurations","text":"res = connected_padded(compiled, states)\nsize(res.configs), size(res.mels), size(res.counts)\n\nconfigs holds the connected configurations as packed integers, mels their matrix elements, and counts how many entries per sample are real rather than padding. The connection axis comes first: NetKet puts it last, but Julia is column-major, and this is what keeps one sample's connections contiguous.\n\nstates may be an array of any shape — the connection axis is prepended and the batch shape preserved — so a (steps, chains) block of Monte Carlo samples goes in without reshaping.","title":"The batched call"},{"category":"section","location":"manual/connectedconfigs/#Two-conventions-that-matter","page":"Connected configurations","text":"Padding is inert. Unused slots carry a matrix element of zero and repeat the sample itself. The zero makes the local energy correct with no masking; the repeat means every padded row is still a valid configuration, so the wavefunction can be evaluated on the whole array without special-casing.\n\nb = argmin(res.counts)\nres.counts[b], res.mels[(res.counts[b]+1):end, b]\n\nRepeated configurations are not merged. Two terms reaching the same configuration produce two rows rather than one summed row, which is what keeps the kernel free of a per-sample hash table. Every consumer sums over the connection axis, so results are unchanged; only the row count differs. Use connected when you want the summed, deduplicated matrix elements as a dictionary.","title":"Two conventions that matter"},{"category":"section","location":"manual/connectedconfigs/#Unpacking-for-a-network","page":"Connected configurations","text":"Packed integers suit the kernel — compact, hashable, cheap to permute. A network wants an explicit numeric array, and configurations is the boundary. The degree-of-freedom axis comes first, so each configuration is contiguous:\n\nsize(configurations(model.dof, res.configs, model.nsites))","title":"Unpacking for a network"},{"category":"section","location":"manual/connectedconfigs/#Full-documentation","page":"Connected configurations","text":"ConnectedConfigs is registered as a package in its own right and has a complete site:\n\nConnected configurations — compiling, batch shapes, padding, and reusing buffers with connected_padded!\nLocal operators — the digit ordering, and why PAULI_Z is the wrong constant to build a Hamiltonian from\nSymmetry sectors — folding onto orbit representatives, characters and norm factors\nBackends — swapping SymBasis or OperatorAlgebra for another library\nExact diagonalization\nAPI reference","title":"Full documentation"},{"category":"section","location":"manual/nqscore/#NQSCore.jl","page":"Variational states","text":"The interface backbone of the neural-quantum-states stack.\n\nNQSCore defines the abstract types and function names that NQSAnsatze, NQSSamplers, and NQSOptimisers are all written against — the role SciMLBase plays for SciML, or ChainRulesCore for the autodiff ecosystem. Because they share this vocabulary, none of them has to depend on any other.\n\nIt also provides the two variational state types, the statistics, and the log-derivative machinery they share.","title":"NQSCore.jl"},{"category":"section","location":"manual/nqscore/#Two-states,-one-interface","page":"Variational states","text":"Type Averages by Error bar\nFullSumState exact summation over the basis none — the answer is exact\nMCState Monte Carlo sampling standard error, autocorrelation-corrected\n\nBoth satisfy the same expect interface, which is what makes the development loop work: build a model against exact summation, where any disagreement is a bug rather than a fluctuation, then scale up by swapping the state type and changing nothing else.","title":"Two states, one interface"},{"category":"section","location":"manual/nqscore/#Reference-implementations","page":"Variational states","text":"LogStateVector and ExactSampler contain no approximation at all. The first carries one parameter per basis state, so it can represent any state exactly; the second enumerates the basis and draws genuinely independent samples. Both are useless for anything large and indispensable for testing — they let the machinery be checked against exact answers rather than against itself.\n\nLogStateVector also has a closed-form Jacobian (log ψ(s) = θ_s, so O is a one-hot indicator matrix), which is how log_derivatives itself is verified.","title":"Reference implementations"},{"category":"section","location":"manual/nqscore/#Statistics","page":"Variational states","text":"Every expect returns a Stats: mean, error of the mean, variance, integrated autocorrelation time, and split-R̂. Downstream code never branches on where a number came from.\n\nThe error bar is corrected for autocorrelation. Markov chain samples are correlated, so the naive std/sqrt(n) understates it by roughly sqrt(tau_corr) — reporting that unadjusted is the most common way to converge confidently to a wrong answer. The energy variance is worth watching in its own right: it vanishes at an exact eigenstate, which makes it the sharpest convergence diagnostic available.","title":"Statistics"},{"category":"section","location":"manual/nqscore/#Log-derivatives","page":"Variational states","text":"log_derivatives computes O[s, k] = ∂ log ψ(x_s) / ∂θ_k, the object behind both the energy gradient (a covariance between O and the local energies) and the quantum geometric tensor that stochastic reconfiguration inverts.\n\nTwo independent things can be complex, and conflating them is the usual source of wrong gradients:\n\nA complex log-amplitude with real parameters — the common case for a network emitting a modulus and a phase. O is complex even though θ is real; both parts come from one autodiff pass over [real(log ψ); imag(log ψ)].\nComplex parameters, which are ambiguous until a convention is fixed. holomorphic=true assumes ψ is holomorphic in θ; the default treats real and imaginary parts as 2n independent real parameters, which is always valid. The holomorphic form is a shortcut that is silently wrong when the ansatz does not satisfy it, so it must be asked for explicitly.\n\nWhatever the parameterization, the gradient handed back always matches the structure of the parameters, so an optimizer never has to ask how the ansatz was parameterized.","title":"Log-derivatives"},{"category":"section","location":"manual/nqscore/#Backends","page":"Variational states","text":"Differentiation goes through DifferentiationInterface.jl, so any of its backends works — AutoForwardDiff(), AutoZygote(), AutoEnzyme(). None of those packages is a dependency here; load the one you want and pass it.","title":"Backends"},{"category":"section","location":"manual/nqscore/#Quick-example","page":"Variational states","text":"using NQSCore, ConnectedConfigs, SymBasis, OperatorAlgebra\nusing DifferentiationInterface, ForwardDiff, Random\n\nspec, nsites = Spin(1 // 2), 4\nb = basis(dof_object(spec), nsites)\n\nops = local_operators(spec)\nH = OpSum([Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites])\n\na = LogStateVector(spec, nsites, b)\nvs = FullSumState(a, init_parameters(a, Xoshiro(0); scale=0.3), AutoForwardDiff())\n\nexpect(vs, H)\n\nstats, gradient = expect_and_grad(vs, H)\nstats","title":"Quick example"},{"category":"section","location":"manual/nqssamplers/#NQSSamplers.jl","page":"Samplers","text":"Monte Carlo sampling over discrete quantum configurations.\n\nMetropolisSampler owns the Markov chain machinery; an AbstractRule owns the physics of which moves to propose. That separation follows NetKet, and it matters because the rule is where all the problem knowledge lives.","title":"NQSSamplers.jl"},{"category":"section","location":"manual/nqssamplers/#Choosing-a-rule","page":"Samplers","text":"This is the decision that determines whether the chain works at all.\n\nSituation Rule\nNo conserved quantity LocalRule\nFixed magnetization or particle number ExchangeRule\nSparse Hamiltonian, hard-to-satisfy constraints HamiltonianRule\n\nwarning: A local rule cannot move inside a conserved sector\nEvery single-site change alters the total, so every proposal leaves the sector and is rejected. The chain then returns one configuration forever — while reporting perfectly reasonable-looking error bars, because a constant sequence has no variance. Use ExchangeRule, which swaps two sites and therefore conserves any quantity that is a sum over sites.\n\nHamiltonianRule proposes only configurations the Hamiltonian actually connects to, so it respects whatever the Hamiltonian conserves without being told what that is. It is asymmetric — a configuration and its image generally have different numbers of connections — so it carries a proposal correction. Dropping that correction biases the sampled distribution while leaving everything else looking fine.","title":"Choosing a rule"},{"category":"section","location":"manual/nqssamplers/#Multiple-chains","page":"Samplers","text":"Not for speed. Several chains started from dispersed configurations give a convergence diagnostic no single chain can: split-R̂ compares them, and a chain stuck in one region is invisible from the inside. The error bar is also estimated from the spread between chain means, which assumes only that the chains are independent rather than relying on an autocorrelation model.\n\nAll chains advance together, so each step evaluates the ansatz once on a batch rather than once per chain — the ansatz being the expensive part of sampling.","title":"Multiple chains"},{"category":"section","location":"manual/nqssamplers/#Diagnostics","page":"Samplers","text":"Check two numbers before believing a result:\n\nr_hat from the returned Stats. Anything much above 1.01 means the chains disagree and the error bar cannot be trusted.\nACCEPTANCE after a run. Near zero means a barely-moving chain; near one usually means proposals too timid to explore. Both produce confident-looking error bars from samples that carry no information.","title":"Diagnostics"},{"category":"section","location":"manual/nqssamplers/#What-is-deliberately-elsewhere","page":"Samplers","text":"Exact summation is a variational state (NQSCore.FullSumState), not a sampler, because it involves no sampling. NQSCore.ExactSampler likewise stays there as the reference implementation these samplers are validated against — a Metropolis chain must reproduce the exact |ψ|², and the test suite checks the total-variation distance between them.","title":"What is deliberately elsewhere"},{"category":"section","location":"manual/nqssamplers/#Quick-example","page":"Samplers","text":"using NQSSamplers, NQSCore, ConnectedConfigs, SymBasis, OperatorAlgebra\nusing DifferentiationInterface, ForwardDiff, Random\n\nspec, nsites = Spin(1 // 2), 6\nb = basis(dof_object(spec), nsites)\na = LogStateVector(spec, nsites, b)\nθ = init_parameters(a, Xoshiro(0); scale=0.4)\n\nops = local_operators(spec)\nH = OpSum(vcat(\n    [Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites],\n    [Op(2 .* ops.sx, i) for i in 1:nsites],\n))\n\nstarts = random_configurations(spec, nsites, 8, Xoshiro(1))\nsampler = MetropolisSampler(LocalRule(), starts;\n    n_chains=8, n_samples=5_000, burn_in=1_000)\n\nvs = MCState(a, θ, sampler; backend=AutoForwardDiff(), rng=Xoshiro(2))\nexpect(vs, H)\n\nCompare against the exact answer for the same parameters:\n\nexpect(FullSumState(a, θ, AutoForwardDiff()), H)","title":"Quick example"},{"category":"section","location":"lib/nqsoptimisers/#NQSOptimisers","page":"NQSOptimisers","text":"See NQSOptimisers.jl in the manual for what this package is for and how to use it.","title":"NQSOptimisers"},{"category":"section","location":"lib/nqsoptimisers/#Index","page":"NQSOptimisers","text":"Pages = [\"nqsoptimisers.md\"]","title":"Index"},{"category":"module","location":"lib/nqsoptimisers/#NQSOptimisers.NQSOptimisers","page":"NQSOptimisers","text":"NQSOptimisers\n\nStochastic reconfiguration and natural-gradient preconditioning for variational quantum states.\n\nThis corresponds to NetKet's netket/_src/ngd together with netket/_src/solvers.\n\nWhy preconditioning is not optional\n\nPlain gradient descent follows the steepest direction in parameter space. That is the wrong geometry for a wavefunction: rescaling two parameters against each other can leave the state untouched while completely changing the gradient. StochasticReconfiguration instead follows the steepest direction in state space, using the quantum geometric tensor as the metric.\n\nThe practical consequence is stark. In the ordered regime of a transverse-field Ising chain, plain descent stalls at the classical energy, because the gradient carries a factor of the Born probability p(s) that vanishes for exactly the configurations whose amplitude must grow. The geometric tensor carries the same factor, and dividing by it undoes the suppression.\n\nWhat is here\n\nStochasticReconfiguration — in both the P × P and the kernel-trick N × N form, which give exactly the same update.\nIdentity — no preconditioning, so plain descent and SR can be compared under identical conditions.\nSolvers: CholeskySolver, PseudoInverseSolver, ConjugateGradientSolver — a real choice, because the geometric tensor is routinely singular.\noptimize! — a minimal driver.\n\nThe solver seam and BatchSolve.jl\n\nLinear solves go through AbstractLinearSolver rather than a hardcoded call, which is where a batched GPU solver would attach as a package extension. That is deliberately not wired up: whether BatchSolve.jl's batched path beats a plain Cholesky or CG on a realistic geometric tensor is an open question, and a batched solver is only a win if the batching pays for itself here. Benchmark before committing to it.\n\nNot yet here\n\nTime evolution (TDVP) uses the same geometric tensor with an imaginary or real time step. It is deliberately left out rather than half-implemented.\n\nExample\n\nusing NQSCore, NQSOptimisers\n\nsr = StochasticReconfiguration(; diag_shift=0.01)\nhistory = optimize!(vs, H, sr; iterations=200, learning_rate=0.05)\n\n\n\n\n\n","title":"NQSOptimisers.NQSOptimisers"},{"category":"type","location":"lib/nqsoptimisers/#NQSOptimisers.AbstractLinearSolver","page":"NQSOptimisers","text":"AbstractLinearSolver\n\nHow to solve the linear system at the heart of a preconditioned update.\n\nEvery stochastic-reconfiguration variant reduces to solving (A + λI) x = b for a symmetric positive-semidefinite A. Keeping that behind an interface is not ceremony: the geometric tensor is routinely singular, because redundant parameter directions and directions no sample explores both produce exact zero modes. Which regularization is used is therefore a real choice with real consequences, not an implementation detail — and it is also the seam where a batched GPU solver such as BatchSolve.jl would attach.\n\nInterface\n\nsolve(solver, A, b, shift) -> x\n\n\n\n\n\n","title":"NQSOptimisers.AbstractLinearSolver"},{"category":"type","location":"lib/nqsoptimisers/#NQSOptimisers.CholeskySolver","page":"NQSOptimisers","text":"CholeskySolver() <: AbstractLinearSolver\n\nDense Cholesky factorization of the shifted system.\n\nThe fastest option when the matrix fits in memory and the shift is large enough to make it positive definite. Falls back to a symmetric indefinite factorization if Cholesky fails, which happens when the shift is too small to cover the zero modes — silently returning garbage there would be worse than the extra cost.\n\n\n\n\n\n","title":"NQSOptimisers.CholeskySolver"},{"category":"type","location":"lib/nqsoptimisers/#NQSOptimisers.ConjugateGradientSolver","page":"NQSOptimisers","text":"ConjugateGradientSolver(; maxiter=1000, tol=1e-10) <: AbstractLinearSolver\n\nIterative solve by conjugate gradients, through KrylovKit.\n\nNever forms a factorization, so it scales to parameter counts where a dense solve is impossible. It needs only the action of the matrix on a vector, which is what makes a matrix-free geometric tensor usable.\n\n\n\n\n\n","title":"NQSOptimisers.ConjugateGradientSolver"},{"category":"type","location":"lib/nqsoptimisers/#NQSOptimisers.Identity","page":"NQSOptimisers","text":"Identity() <: AbstractPreconditioner\n\nNo preconditioning: the update is the plain energy gradient.\n\nPresent so that a driver can treat plain gradient descent and stochastic reconfiguration uniformly, and so that the two can be compared under otherwise identical conditions.\n\n\n\n\n\n","title":"NQSOptimisers.Identity"},{"category":"type","location":"lib/nqsoptimisers/#NQSOptimisers.PseudoInverseSolver","page":"NQSOptimisers","text":"PseudoInverseSolver(; rtol=1e-10) <: AbstractLinearSolver\n\nMoore–Penrose pseudo-inverse via SVD, discarding singular values below rtol times the largest.\n\nThe most robust option, and the one to reach for when the geometric tensor is genuinely singular. Rather than inflating the small singular values as a diagonal shift does, this projects out the corresponding directions entirely: the update simply makes no progress along directions the samples carry no information about, instead of making a wildly large and arbitrary move along them.\n\nCosts an SVD, so it is for small parameter counts or for diagnosis.\n\n\n\n\n\n","title":"NQSOptimisers.PseudoInverseSolver"},{"category":"type","location":"lib/nqsoptimisers/#NQSOptimisers.StochasticReconfiguration","page":"NQSOptimisers","text":"StochasticReconfiguration(; diag_shift=0.01, diag_scale=0.0, solver=CholeskySolver(),\n                            mode=:auto, holomorphic=false) <: AbstractPreconditioner\n\nStochastic reconfiguration, also known as the natural gradient or imaginary-time projection.\n\nPlain gradient descent follows the steepest direction in parameter space, which is the wrong geometry: two parameters can be scaled arbitrarily relative to one another without changing the wavefunction at all. Stochastic reconfiguration instead follows the steepest direction in state space, by preconditioning the gradient with the quantum geometric tensor\n\nS_kk = mathrmRe left langle O_k^* O_k rangle\n          - langle O_k^* rangle langle O_k rangle right\n\nand solving S δ = ∇E for the update direction.\n\nwarning: Use a smaller step than for plain descent\nThe preconditioned update is routinely one to two orders of magnitude larger than the raw gradient, so a learning rate that is fine for plain gradient descent will overshoot here and can leave the run stalled at a non-eigenstate. Start around 0.05 with diag_shift = 0.01 and reduce if the energy plateaus above the ground state while its variance stays large.\n\nThis is what rescues the ordered regime. Plain descent stalls there because the gradient carries a factor of the Born probability p(s), which vanishes for exactly the configurations whose amplitude needs to grow; S carries the same factor and dividing by it undoes the suppression.\n\nFields\n\ndiag_shift: absolute regularization λ added to the diagonal. The geometric tensor is routinely singular — redundant parameters and unexplored directions both give exact zero modes — so some regularization is mandatory rather than optional.\ndiag_scale: regularization relative to each diagonal entry, which tracks the tensor's own magnitude as it changes during a run. See _regularize.\nsolver: an AbstractLinearSolver.\nmode: :sr builds the P × P matrix S, :minsr builds the N × N matrix instead (see below), and :auto picks whichever is smaller.\nholomorphic: passed through to log_derivatives.\n\nThe two forms are the same update\n\nWith X the centered, probability-weighted log-derivative matrix split into real and imaginary parts and stacked, S = XᵀX and ∇E = 2Xᵀε. The identity\n\n(X^T X + lambda I)^-1 X^T = X^T (X X^T + lambda I)^-1\n\nmeans the update can be computed either from the P × P matrix XᵀX or from the N × N matrix XXᵀ. They agree exactly — not approximately — so the choice is purely one of cost. For a neural network with far more parameters than samples, which is the usual case, the second is dramatically cheaper. That form is the kernel trick, known in this context as MinSR or SRt.\n\n\n\n\n\n","title":"NQSOptimisers.StochasticReconfiguration"},{"category":"method","location":"lib/nqsoptimisers/#NQSOptimisers._regularize-Tuple{AbstractMatrix, Real, Real}","page":"NQSOptimisers","text":"_regularize(A, shift, scale) -> A + shift*I + scale*Diagonal(diag(A))\n\nApply both forms of regularization.\n\nshift is an absolute addition to the diagonal, and scale is one relative to each diagonal entry (Sorella's original prescription). A relative shift tracks the tensor's own magnitude, which changes over the course of an optimization, so a value chosen at the start does not become negligible or overwhelming later.\n\ndiag_scale defaults to zero — the absolute shift alone is the common case, and the relative form is offered rather than imposed. Increasing either one damps the update.\n\nnote: Regularization is not a substitute for a sensible step size\nThe geometric tensor is genuinely ill-conditioned — condition numbers of 1e15 are ordinary, since redundant parameter directions give exact zero modes — but that does not by itself make the update wrong. A run that stalls at a non-eigenstate while the gradient stays perfectly ordinary is usually overshooting: the preconditioned update can be two orders of magnitude larger than the gradient, so a learning rate tuned for plain descent is far too large for stochastic reconfiguration. Reach for a smaller step or a larger shift before concluding the conditioning is at fault.\n\n\n\n\n\n","title":"NQSOptimisers._regularize"},{"category":"method","location":"lib/nqsoptimisers/#NQSOptimisers._weighted_design-Tuple{AbstractMatrix, AbstractVector, Any}","page":"NQSOptimisers","text":"_weighted_design(O, E, weights) -> (X, ε)\n\nThe real design matrix X and residual ε that both forms of the update are built from.\n\nX is the centered log-derivative matrix scaled by sqrt(p), with its real and imaginary parts stacked into a 2N × P real matrix; ε is the correspondingly stacked centered local energy. Stacking rather than working in complex arithmetic is what makes Re[X†X] = XᵀX literally true, so the kernel-trick identity applies without any special-casing.\n\nCentering is not optional: it removes the direction corresponding to a global change of normalization, which is unphysical and would otherwise be an exact zero mode of the geometric tensor.\n\n\n\n\n\n","title":"NQSOptimisers._weighted_design"},{"category":"method","location":"lib/nqsoptimisers/#NQSOptimisers.optimize!-Tuple{AbstractVariationalState, Any, AbstractPreconditioner}","page":"NQSOptimisers","text":"optimize!(state, operator, preconditioner; iterations, learning_rate, callback=nothing)\n    -> Vector{Stats}\n\nRun a variational optimization, returning the energy at every step.\n\nThis is the smallest useful driver: no logging, no checkpointing, no early stopping. Those belong with the umbrella package; what is here is enough to test that a preconditioner actually descends.\n\ncallback(iteration, stats, state) runs after each step if given.\n\n\n\n\n\n","title":"NQSOptimisers.optimize!"},{"category":"method","location":"lib/nqsoptimisers/#NQSOptimisers.precondition-Tuple{StochasticReconfiguration, AbstractVariationalState, Any}","page":"NQSOptimisers","text":"precondition(sr, state, operator) -> (Stats, update)\n\nThe stochastic-reconfiguration update direction, together with the energy it was computed from.\n\nupdate has the same structure as the state's parameters, so a step is θ .- η .* update (or fmap over a nested container). Both returned values come from a single set of samples.\n\n\n\n\n\n","title":"NQSOptimisers.precondition"},{"category":"function","location":"lib/nqsoptimisers/#NQSOptimisers.solve","page":"NQSOptimisers","text":"solve(solver, A, b, shift) -> x\n\nSolve (A + shift*I) x = b.\n\n\n\n\n\n","title":"NQSOptimisers.solve"},{"category":"section","location":"lib/nqssamplers/#NQSSamplers","page":"NQSSamplers","text":"See NQSSamplers.jl in the manual for what this package is for and how to use it.","title":"NQSSamplers"},{"category":"section","location":"lib/nqssamplers/#Index","page":"NQSSamplers","text":"Pages = [\"nqssamplers.md\"]","title":"Index"},{"category":"module","location":"lib/nqssamplers/#NQSSamplers.NQSSamplers","page":"NQSSamplers","text":"NQSSamplers\n\nMonte Carlo samplers over discrete quantum configurations.\n\nThis corresponds to NetKet's netket/sampler, including its rules/ submodule, and follows the same separation: the MetropolisSampler owns the Markov chain machinery, while an AbstractRule owns the physics of which moves to propose.\n\nWhat is here\n\nMetropolisSampler — multi-chain Metropolis–Hastings with burn-in and thinning.\nRules: LocalRule for unconstrained systems, ExchangeRule for conserved sectors, and HamiltonianRule for proposals drawn from the Hamiltonian's own connectivity.\n\nExact summation lives elsewhere on purpose: following NetKet, NQSCore.FullSumState is a variational state rather than a sampler, because it involves no sampling at all. NQSCore.ExactSampler likewise stays there as the reference implementation that these samplers are validated against.\n\nChoosing a rule\n\nThe rule is the part that has to match the problem:\n\nSituation Rule\nNo conserved quantity LocalRule\nFixed magnetization or particle number ExchangeRule\nSparse Hamiltonian, hard-to-satisfy constraints HamiltonianRule\n\nUsing LocalRule inside a conserved sector is the classic mistake: every proposal leaves the sector, everything is rejected, and the chain returns the same configuration forever with error bars that look perfectly reasonable.\n\nExample\n\nusing NQSCore, NQSSamplers\n\nstarts = random_configurations(dof, nsites, 8, rng)\nsampler = MetropolisSampler(LocalRule(), starts; n_chains=8, n_samples=2000, burn_in=500)\nvs = MCState(ansatz, θ, sampler; backend=AutoForwardDiff(), rng=rng)\nexpect(vs, H)\n\n\n\n\n\n","title":"NQSSamplers.NQSSamplers"},{"category":"constant","location":"lib/nqssamplers/#NQSSamplers.ACCEPTANCE","page":"NQSSamplers","text":"ACCEPTANCE\n\nAcceptance rate of the most recent MetropolisSampler run.\n\nA diagnostic, not part of the interface: a rate near zero means the chain is barely moving and the samples are effectively one configuration repeated, while a rate near one usually means the proposals are too timid to explore. Both produce error bars that look fine and mean nothing.\n\n\n\n\n\n","title":"NQSSamplers.ACCEPTANCE"},{"category":"type","location":"lib/nqssamplers/#NQSSamplers.AbstractRule","page":"NQSSamplers","text":"AbstractRule\n\nA Metropolis transition rule: how to propose a move from one configuration to another.\n\nSeparating the rule from the Metropolis machinery is what NetKet does, and the reason is that the rule is where all the physics knowledge lives. Which moves are proposed determines whether the chain explores the relevant part of configuration space at all — and, critically, whether it stays inside a conserved-quantity sector.\n\nInterface\n\npropose(rule, state, dof, nsites, rng) -> (new_state, log_correction)\n\nlog_correction is log[T(s|s') / T(s'|s)], the asymmetry of the proposal. It is zero for a symmetric rule and must be accounted for otherwise, or the chain converges to the wrong distribution.\n\n\n\n\n\n","title":"NQSSamplers.AbstractRule"},{"category":"type","location":"lib/nqssamplers/#NQSSamplers.ExchangeRule","page":"NQSSamplers","text":"ExchangeRule() <: AbstractRule\n\nSwap the values on two randomly chosen sites.\n\nConserves any quantity that is a sum over sites — total magnetization, total particle number — because swapping leaves that sum untouched. This is the rule to use inside a symmetry sector, where LocalRule would propose nothing but rejections.\n\nSymmetric, so the proposal correction is zero.\n\nnote: It cannot leave its sector\nThat is the point, but it also means the chain only ever explores the sector it starts in. The initial configuration therefore determines which sector is sampled.\n\n\n\n\n\n","title":"NQSSamplers.ExchangeRule"},{"category":"type","location":"lib/nqssamplers/#NQSSamplers.HamiltonianRule","page":"NQSSamplers","text":"HamiltonianRule(operator) <: AbstractRule\n\nPropose moves along the configurations the Hamiltonian actually connects to.\n\nThe most physically informed rule available: it never proposes a move the Hamiltonian gives zero amplitude to, so it automatically respects whatever the Hamiltonian conserves without being told what that is. For a sparse Hamiltonian it explores far more efficiently than blind local moves.\n\nThe price is that the proposal is asymmetric — s may have a different number of connections than s' — so the correction log[n_conn(s) / n_conn(s')] is non-zero and must be carried into the acceptance test. Dropping it silently biases the sampled distribution, which is exactly the kind of error that produces plausible-looking but wrong expectation values.\n\nThe operator is compiled once, when the rule is constructed. Every Metropolis step queries it twice — forwards and backwards — so leaving that work in the step is the difference between flattening the Hamiltonian a handful of times and flattening it a few million.\n\n\n\n\n\n","title":"NQSSamplers.HamiltonianRule"},{"category":"type","location":"lib/nqssamplers/#NQSSamplers.LocalRule","page":"NQSSamplers","text":"LocalRule() <: AbstractRule\n\nChange the value at a single randomly chosen site to a different randomly chosen local value.\n\nThe workhorse rule for unconstrained systems. It is symmetric — every move has an equally likely reverse — so the proposal correction is zero.\n\nIt does not conserve particle number or magnetization. Using it inside a conserved sector proposes moves that leave the sector; those get rejected by the basis check, and the chain stalls. Use ExchangeRule there instead.\n\n\n\n\n\n","title":"NQSSamplers.LocalRule"},{"category":"type","location":"lib/nqssamplers/#NQSSamplers.MetropolisSampler","page":"NQSSamplers","text":"MetropolisSampler(rule, initial; n_chains, n_samples, burn_in, thinning, basis=nothing)\n\nMetropolis–Hastings sampling of |ψ(s)|² over discrete configurations.\n\nFields\n\nrule: an AbstractRule supplying proposals.\ninitial: a configuration to start every chain from, or a vector of one per chain.\nn_chains::Int: chains advanced in parallel.\nn_samples::Int: samples per chain kept after burn-in and thinning.\nburn_in::Int: steps discarded at the start of each chain.\nthinning::Int: steps taken between kept samples.\nbasis: if given, moves leaving this basis are rejected — how a sampler is confined to a symmetry sector.\n\nWhy multiple chains\n\nNot for speed. Several chains started from different configurations give a convergence diagnostic that no single chain can: split-R̂ compares them, and a chain stuck in one region of configuration space is invisible from the inside. The error bar is also estimated from the spread between chain means, which assumes only that the chains are independent, rather than from an autocorrelation model.\n\nBatching\n\nAll chains advance together, so every step evaluates the ansatz once on a batch of n_chains configurations rather than once per chain. The ansatz is the expensive part of sampling, and a neural network is far more efficient on a batch than on single configurations.\n\n\n\n\n\n","title":"NQSSamplers.MetropolisSampler"},{"category":"method","location":"lib/nqssamplers/#NQSSamplers._admissible-Tuple{Nothing, Any}","page":"NQSSamplers","text":"Whether a proposed configuration is admissible: inside the basis, when one is given.\n\n\n\n\n\n","title":"NQSSamplers._admissible"},{"category":"method","location":"lib/nqssamplers/#NQSSamplers._log_prob-Tuple{AbstractAnsatz, Any, AbstractVector}","page":"NQSSamplers","text":"Log of |ψ|² for a batch of packed configurations.\n\n\n\n\n\n","title":"NQSSamplers._log_prob"},{"category":"function","location":"lib/nqssamplers/#NQSSamplers.propose","page":"NQSSamplers","text":"propose(rule, state, dof, nsites, rng) -> (new_state, log_correction)\n\nPropose a move away from state.\n\n\n\n\n\n","title":"NQSSamplers.propose"},{"category":"method","location":"lib/nqssamplers/#NQSSamplers.random_configuration-Tuple{Any, Integer, Random.AbstractRNG}","page":"NQSSamplers","text":"random_configuration(dof, nsites, rng) -> BaseInt\n\nA uniformly random configuration, for starting a chain.\n\n\n\n\n\n","title":"NQSSamplers.random_configuration"},{"category":"method","location":"lib/nqssamplers/#NQSSamplers.random_configurations-Tuple{Any, Integer, Integer, Random.AbstractRNG}","page":"NQSSamplers","text":"random_configurations(dof, nsites, n, rng) -> Vector\n\nn independent random configurations — dispersed chain starts, which is what makes split-R̂ informative rather than vacuous.\n\n\n\n\n\n","title":"NQSSamplers.random_configurations"},{"category":"section","location":"manual/nqsansatze/#NQSAnsatze.jl","page":"Ansätze","text":"Neural-network ansätze for quantum many-body wavefunctions.\n\nLuxAnsatz makes any Lux.jl model usable as a variational wavefunction, and the layers here are wavefunction architectures in their own right.\n\nThis is the one package in the stack that depends on Lux. Everything else reaches neural networks through the NQSCore interface, which is why a sampler or an optimizer never has to know a network is involved.","title":"NQSAnsatze.jl"},{"category":"section","location":"manual/nqsansatze/#The-ansätze","page":"Ansätze","text":"Ansatz Captures\nRBM The canonical neural quantum state; hidden units summed analytically\nSymmetricRBM An RBM whose weights are shared across a symmetry group\nJastrow Pair correlations exactly, and nothing beyond them","title":"The ansätze"},{"category":"section","location":"manual/nqsansatze/#Complex-parameters-by-default","page":"Ansätze","text":"Parameters default to ComplexF64. A wavefunction has a phase, and a real-parameter network can only represent a positive one — which fails silently for any model with a sign structure, such as a frustrated magnet. A real network can still be used by having it emit two rows, read as the real and imaginary parts of log ψ; see LuxAnsatz.","title":"Complex parameters by default"},{"category":"section","location":"manual/nqsansatze/#Symmetry-by-construction","page":"Ansätze","text":"SymmetricRBM applies each hidden filter to every permuted copy of the input, so |ψ| is invariant under the group by construction rather than by training. The parameter count drops by roughly the order of the group, and — more importantly — the ansatz cannot spend capacity representing states the ground state is known not to occupy.\n\nLoading LatticeSpaceGroups activates an extension that derives the permutations from lattice geometry, so a translation-invariant ansatz on a kagome torus is no harder to write than one on a chain:\n\nusing NQSAnsatze, LatticeSpaceGroups\n\nlat = build(Kagome([3, 3], 1.0; periodic=true))\nmodel = SymmetricRBM(lat, 2)      # invariant under every lattice translation","title":"Symmetry by construction"},{"category":"section","location":"manual/nqsansatze/#Numerical-care","page":"Ansätze","text":"log(2 cosh z) is evaluated as logtwocosh, which factors out the dominant exponential rather than computing the expression literally. A literal log(2cosh(z)) overflows to Inf once |Re z| passes about 710 — which an RBM reaches routinely as its weights grow — and one Inf poisons the whole batch rather than a single sample.","title":"Numerical care"},{"category":"section","location":"manual/nqsansatze/#Quick-example","page":"Ansätze","text":"using NQSAnsatze, NQSCore, ConnectedConfigs, SymBasis, OperatorAlgebra\nusing DifferentiationInterface, ForwardDiff, Random\n\nspec, nsites = Spin(1 // 2), 10\nb = basis(dof_object(spec), nsites)\n\na = LuxAnsatz(RBM(nsites, 2), spec, nsites; rng=Xoshiro(0))\nNQSCore.n_parameters(a), length(b.states)\n\nAn RBM's parameter count grows quadratically in the system size while the Hilbert space grows exponentially, so the two cross over quickly — at six sites an alpha = 2 RBM actually has more parameters than there are basis states, and only past roughly eight does it start compressing. That crossover is why an RBM is worth nothing on a toy system and everything on a real one.\n\nops = local_operators(spec)\nH = OpSum(vcat(\n    [Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites],\n    [Op(2 .* ops.sx, i) for i in 1:nsites],\n))\n\nvs = FullSumState(a, init_parameters(a, Xoshiro(0)), AutoForwardDiff())\nexpect(vs, H)","title":"Quick example"},{"category":"section","location":"lib/nqsansatze/#NQSAnsatze","page":"NQSAnsatze","text":"See NQSAnsatze.jl in the manual for what this package is for and how to use it.","title":"NQSAnsatze"},{"category":"section","location":"lib/nqsansatze/#Index","page":"NQSAnsatze","text":"Pages = [\"nqsansatze.md\"]","title":"Index"},{"category":"module","location":"lib/nqsansatze/#NQSAnsatze.NQSAnsatze","page":"NQSAnsatze","text":"NQSAnsatze\n\nNeural-network ansätze for quantum many-body wavefunctions, built on Lux.jl.\n\nThis corresponds to NetKet's netket/nn and netket/models: LuxAnsatz is the bridge that makes any Lux model usable as a wavefunction, and the layers here are the wavefunction architectures themselves.\n\nThis is the one package in the stack allowed to depend on Lux. Everything else reaches neural networks through the NQSCore interface, which is why a sampler or an optimizer never has to know that a network is involved at all.\n\nAnsätze\n\nRBM — the canonical neural quantum state, with its hidden units summed analytically.\nSymmetricRBM — weights shared across a symmetry group, so |ψ| is invariant by construction rather than by training.\nJastrow — a pair-correlation factor.\n\nComplex wavefunctions\n\nParameters default to ComplexF64. A wavefunction has a phase, and a real-parameter network can only represent a positive one — which silently fails for any model with a sign structure, such as a frustrated magnet. A real network can still be used by having it return two rows, read as the real and imaginary parts of log ψ; see LuxAnsatz.\n\nExample\n\nusing NQSAnsatze, NQSCore\n\nmodel = RBM(nsites, 2)\na = LuxAnsatz(model, dof, nsites; rng)\nvs = FullSumState(a, init_parameters(a, rng); backend=AutoZygote())\n\nLoading LatticeSpaceGroups alongside activates an extension that builds symmetric ansätze straight from a lattice, deriving the permutations from its geometry.\n\n\n\n\n\n","title":"NQSAnsatze.NQSAnsatze"},{"category":"type","location":"lib/nqsansatze/#NQSAnsatze.Jastrow","page":"NQSAnsatze","text":"Jastrow(nsites; T=ComplexF64) <: Lux.AbstractLuxLayer\n\nA Jastrow factor: log ψ(x) = Σ_{i<j} J_{ij} x_i x_j.\n\nCaptures pair correlations exactly and nothing beyond them. Cheap, interpretable, and a standard component to multiply into a richer ansatz rather than to use alone.\n\nOnly the strict upper triangle is parameterized: J_{ij} and J_{ji} would multiply the same product x_i x_j, so carrying both makes the parameterization redundant and the geometric tensor singular.\n\n\n\n\n\n","title":"NQSAnsatze.Jastrow"},{"category":"type","location":"lib/nqsansatze/#NQSAnsatze.LuxAnsatz","page":"NQSAnsatze","text":"LuxAnsatz(model, dof, nsites; rng, states=nothing) <: AbstractAnsatz\n\nWraps any Lux model as a variational wavefunction.\n\nThis is the bridge between the two worlds: NQSCore asks for log_amplitude(ansatz, θ, x) and knows nothing about Lux, while Lux models want Lux.apply(model, x, ps, st). Everything Lux offers — layer composition, GPU movement, the whole ecosystem — is available through this one type, and NQSCore keeps no dependency on it.\n\nOutput convention\n\nThe model must return either\n\na length-batch vector of log-amplitudes, which the layers here do; or\na (2, batch) array, whose two rows are read as the real and imaginary parts of log ψ.\n\nThe second form is how a real-valued network represents a complex wavefunction, and it replaces the ComplexF32[1 im] * model(x) idiom that was scattered through the pre-split scratch code.\n\nInput conversion\n\nConfigurations arrive as physical local values — Rational for spins — which no neural network can consume. They are converted to the real element type matching the parameters, once, at this boundary. Doing it here rather than inside each layer means a layer never has to think about it.\n\nFields\n\nmodel: the Lux layer or chain.\nstates: Lux layer states (empty for the layers here, which are all stateless).\ndof, nsites: the space the wavefunction is defined on.\n\n\n\n\n\n","title":"NQSAnsatze.LuxAnsatz"},{"category":"type","location":"lib/nqsansatze/#NQSAnsatze.RBM","page":"NQSAnsatze","text":"RBM(nsites, alpha; T=ComplexF64, init=nothing) <: Lux.AbstractLuxLayer\n\nA restricted Boltzmann machine, the canonical neural quantum state.\n\nlog psi(x) = sum_i a_i x_i + sum_j log left( 2 cosh left( b_j\n                 + sum_i W_ji x_i right) right)\n\nThe hidden units are summed analytically rather than sampled — that closed form is what makes an RBM usable as a wavefunction at all, and it is why this is a bespoke layer rather than a Dense composed with something.\n\nalpha is the hidden-unit density: there are alpha * nsites hidden units.\n\nComplex parameters by default\n\nT defaults to ComplexF64 because a wavefunction has a phase, and a real RBM can only represent a positive one. Complex weights give the modulus and the phase together from a single network, rather than needing two.\n\nlog(2cosh(z)) is evaluated as logtwocosh rather than literally, since cosh overflows for |z| beyond about 710 and the layer would otherwise return Inf on perfectly ordinary inputs.\n\n\n\n\n\n","title":"NQSAnsatze.RBM"},{"category":"type","location":"lib/nqsansatze/#NQSAnsatze.SymmetricRBM","page":"NQSAnsatze","text":"SymmetricRBM(permutations, alpha; T=ComplexF64) <: Lux.AbstractLuxLayer\n\nAn RBM whose weights are shared across a symmetry group, so that |ψ| is invariant under it.\n\npermutations is a vector of site permutations — exactly what LatticeSpaceGroups.site_permutation produces — and each hidden filter is applied to every permuted copy of the input. The resulting wavefunction respects the symmetry by construction, rather than having to learn it.\n\nThat matters for two reasons. The parameter count drops by roughly the order of the group, and more importantly the ansatz cannot waste capacity representing states the ground state is known not to occupy. This is the payoff of deriving symmetry permutations from lattice geometry: the same code gives a translation-invariant ansatz on a kagome torus as on a chain.\n\n\n\n\n\n","title":"NQSAnsatze.SymmetricRBM"},{"category":"method","location":"lib/nqsansatze/#NQSAnsatze._as_log_amplitude-Tuple{AbstractVector}","page":"NQSAnsatze","text":"Interpret a model's output as a vector of complex log-amplitudes.\n\n\n\n\n\n","title":"NQSAnsatze._as_log_amplitude"},{"category":"method","location":"lib/nqsansatze/#NQSAnsatze._input_type-Tuple{Any}","page":"NQSAnsatze","text":"Real element type to feed the network, matching whatever the parameters are made of.\n\n\n\n\n\n","title":"NQSAnsatze._input_type"},{"category":"method","location":"lib/nqsansatze/#NQSAnsatze.logtwocosh-Tuple{Number}","page":"NQSAnsatze","text":"logtwocosh(z)\n\nlog(2 cosh z), computed without overflowing.\n\nFactoring out the dominant exponential, 2cosh(z) = e^{z} + e^{-z} = e^{a}(e^{z-a} + e^{-z-a}) with a = |Re z|, keeps both exponentials bounded by 1 in modulus. A literal log(2cosh(z)) overflows to Inf once |Re z| passes about 710, which an RBM reaches routinely as its weights grow during optimization — and an Inf log-amplitude poisons the entire batch rather than one sample.\n\n\n\n\n\n","title":"NQSAnsatze.logtwocosh"},{"category":"function","location":"lib/nqsansatze/#NQSCore.init_parameters","page":"NQSAnsatze","text":"init_parameters(ansatz, rng) -> NamedTuple\n\nInitial parameters for a LuxAnsatz, from the model's own initialization.\n\n\n\n\n\n","title":"NQSCore.init_parameters"},{"category":"section","location":"#NeuralQuantumStates.jl","page":"Home","text":"Neural quantum states in Julia.\n\nNeuralQuantumStates.jl facilitates the training of neural quantum states (NQS) by variational Monte Carlo (VMC). It aims to provide an efficient and extensible environment for simulating closed many-body quantum systems, taking inspiration from NetKet and jVMC.\n\nwarning: Work in progress\nThis package is under active development and is being reorganized (see below). Most functionality is still being implemented, performance is not yet optimized for CPU or GPU, and the API is unstable.","title":"NeuralQuantumStates.jl"},{"category":"section","location":"#The-package-ecosystem","page":"Home","text":"NeuralQuantumStates.jl is being split from a single monolithic package into a set of focused packages, so that each piece can be used on its own without paying for the whole stack. In Julia this matters more than it does in Python: depending on a package means paying its compile latency, so a user who only wants lattice geometry should not have to load an autodiff engine and a GPU backend.\n\nThe packages are organized in two tiers.\n\nTier 1 — no machine-learning dependencies. Usable on their own for plain exact diagonalization.\n\nPackage Role\nSymBasis.jl States, degrees of freedom, symmetry groups, symmetry-reduced bases\nOperatorAlgebra.jl Operator algebra: Op, OpChain, OpSum, sparse/dense conversion, fermionic sites\nLatticeSpaceGroups Lattice geometry, bonds, and the site permutations symmetry groups need\nConnectedConfigs The batched local-energy kernel: connected configurations and their matrix elements\n\nTier 2 — the neural-network stack.\n\nPackage Role\nNQSCore Interfaces, the MCState and FullSumState variational states, log-derivatives, statistics\nNQSAnsatze Ansätze built on Lux: RBM, symmetric RBM, Jastrow\nNQSSamplers Metropolis sampling with local, exchange and Hamiltonian transition rules\nNQSOptimisers Stochastic reconfiguration and its kernel-trick (MinSR) form\n\nNeuralQuantumStates.jl itself is the umbrella: it re-exports all of the above and adds the predefined models and the VMC driver with callbacks and logging. Installing it gives you the whole stack.","title":"The package ecosystem"},{"category":"section","location":"#Installation","page":"Home","text":"import Pkg; Pkg.add(url=\"https://github.com/cevenkadir/NeuralQuantumStates.jl\")","title":"Installation"},{"category":"section","location":"#Getting-started","page":"Home","text":"See Basics for a worked example that builds a lattice, a Hilbert space, and a Hamiltonian, and then inspects its connected basis configurations.\n\nLatticeSpaceGroups and ConnectedConfigs are registered as packages in their own right and carry their own documentation sites. The Manual has a chapter for each — Lattices and symmetries and Connected configurations — covering what you need to use them from here, and linking on to the full sites for the rest.","title":"Getting started"},{"category":"section","location":"#Bug-reports-and-feature-requests","page":"Home","text":"Please open an issue.","title":"Bug reports and feature requests"},{"category":"section","location":"#Citation","page":"Home","text":"If you use this package in your work, we would appreciate the reference in CITATION.bib.","title":"Citation"},{"category":"section","location":"manual/nqsoptimisers/#NQSOptimisers.jl","page":"Optimisers","text":"Stochastic reconfiguration and natural-gradient preconditioning.\n\nPlain gradient descent follows the steepest direction in parameter space. For a wavefunction that is the wrong geometry: two parameters can be rescaled against each other without changing the state at all, while completely changing the gradient. StochasticReconfiguration follows the steepest direction in state space instead, using the quantum geometric tensor\n\nS_kk = mathrmRe left langle O_k^* O_k rangle\n          - langle O_k^* rangle langle O_k rangle right\n\nas the metric, and solving S delta = nabla E.","title":"NQSOptimisers.jl"},{"category":"section","location":"manual/nqsoptimisers/#Why-this-is-not-optional","page":"Optimisers","text":"In the ordered regime of a transverse-field Ising chain, plain descent stalls at the classical energy. The reason is structural rather than a matter of tuning: the gradient carries a factor of the Born probability p(s), which vanishes for exactly the configurations whose amplitude needs to grow. The geometric tensor carries the same factor, and dividing by it undoes the suppression.\n\nOn a 4-site chain at J = 1, h_x = 09:\n\n iterations gap to the exact ground state\nplain gradient descent 5000 0.98\nstochastic reconfiguration 200 0.0 (machine precision)","title":"Why this is not optional"},{"category":"section","location":"manual/nqsoptimisers/#Two-forms-of-the-same-update","page":"Optimisers","text":":sr builds the P times P matrix X^T X; :minsr builds the N times N matrix X X^T. The identity\n\n(X^T X + lambda I)^-1 X^T = X^T (X X^T + lambda I)^-1\n\nmeans they give exactly the same update — not approximately — so the choice is purely one of cost. For a network with far more parameters than samples, which is the usual case, the second is dramatically cheaper. That is the kernel trick, known here as MinSR or SRt. :auto picks whichever matrix is smaller.","title":"Two forms of the same update"},{"category":"section","location":"manual/nqsoptimisers/#Regularization,-and-what-it-is-not-for","page":"Optimisers","text":"The geometric tensor is routinely singular: redundant parameter directions and directions no sample explores both give exact zero modes, and condition numbers of 1e15 are ordinary. Some regularization is therefore mandatory — diag_shift adds an absolute amount to the diagonal, diag_scale an amount relative to each diagonal entry.\n\nwarning: Use a smaller step than for plain descent\nThe preconditioned update is routinely one to two orders of magnitude larger than the raw gradient. A learning rate that is fine for plain gradient descent will overshoot here and can leave the run plateaued at a non-eigenstate — with a perfectly ordinary gradient, which makes it look like ill-conditioning when it is not. Start near 0.05 with diag_shift = 0.01, and reduce the step if the energy plateaus above the ground state while its variance stays large.","title":"Regularization, and what it is not for"},{"category":"section","location":"manual/nqsoptimisers/#Solvers","page":"Optimisers","text":"Which one matters, because the system being solved is genuinely singular.\n\nSolver Behaviour on zero modes\nCholeskySolver Fast; falls back to an indefinite factorization if the shift is too small\nPseudoInverseSolver Projects out small singular directions rather than inflating them\nConjugateGradientSolver Iterative; needs only matrix-vector products, so it scales\n\nThe AbstractLinearSolver interface is also the seam where a batched GPU solver such as BatchSolve.jl would attach. That is deliberately not wired up: whether a batched path beats a plain Cholesky or CG on a realistic geometric tensor is an open question, and batching is only a win if it pays for itself here.","title":"Solvers"},{"category":"section","location":"manual/nqsoptimisers/#Not-yet-here","page":"Optimisers","text":"Time evolution (TDVP) uses the same geometric tensor with a real or imaginary time step. It is left out rather than shipped half-done.","title":"Not yet here"},{"category":"section","location":"manual/nqsoptimisers/#Quick-example","page":"Optimisers","text":"using NQSOptimisers, NQSCore, ConnectedConfigs, SymBasis, OperatorAlgebra\nusing DifferentiationInterface, ForwardDiff, Random, LinearAlgebra\n\nspec, nsites = Spin(1 // 2), 4\nb = basis(dof_object(spec), nsites)\nops = local_operators(spec)\nH = OpSum(vcat(\n    [Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites],\n    [0.9 * Op(2 .* ops.sx, i) for i in 1:nsites],\n))\n\na = LogStateVector(spec, nsites, b)\nvs = FullSumState(a, init_parameters(a, Xoshiro(11); scale=0.3), AutoForwardDiff())\n\noptimize!(vs, H, StochasticReconfiguration(; diag_shift=1e-2);\n    iterations=600, learning_rate=0.05)\nexpect(vs, H)","title":"Quick example"}]
}
