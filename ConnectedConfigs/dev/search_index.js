var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/exact_diagonalization/#Exact-diagonalization","page":"Exact diagonalization","text":"The kernel produces a Hamiltonian one column at a time, which is exactly what building a sparse matrix needs. Doing it this way also exercises the same code path variational Monte Carlo uses, so agreement with a known spectrum is evidence about the kernel and not only about the model.","title":"Exact diagonalization"},{"category":"section","location":"examples/exact_diagonalization/#Building-the-matrix","page":"Exact diagonalization","text":"Each sample contributes one column: res.configs[j, b] is a row index once looked up in the basis, and res.mels[j, b] is the entry. Contributions are added, not assigned, because two terms may reach the same configuration.\n\nusing ConnectedConfigs, OperatorAlgebra, SymBasis, LinearAlgebra\n\nfunction hamiltonian_matrix(H, spec, nsites)\n    b = basis(dof_object(spec), nsites)\n    index = Dict(s => i for (i, s) in pairs(b.states))\n\n    res = connected_padded(compile(H), b.states)\n    M = zeros(ComplexF64, length(b.states), length(b.states))\n    for n in eachindex(b.states), j in 1:res.counts[n]\n        M[index[res.configs[j, n]], n] += res.mels[j, n]\n    end\n    return M\nend\nnothing # hide","title":"Building the matrix"},{"category":"section","location":"examples/exact_diagonalization/#A-case-with-a-known-answer","page":"Exact diagonalization","text":"Non-interacting spins in a transverse field: H = h_x sum_i sigma^x_i has eigenvalues h_x (2k - N) with multiplicity binomNk, because the sigma^x eigenvalues are pm 1 independently on every site.\n\nspec, nsites, h_x = Spin(1 // 2), 6, 0.8\nops = local_operators(spec)\n\nH = OpSum([h_x * Op(2 .* ops.sx, i) for i in 1:nsites])\nM = hamiltonian_matrix(H, spec, nsites)\n\nfound = sort(real(eigvals(Hermitian(M))))\nexpected = sort([h_x * (2k - nsites) for k in 0:nsites for _ in 1:binomial(nsites, k)])\n\nfound ≈ expected","title":"A case with a known answer"},{"category":"section","location":"examples/exact_diagonalization/#The-transverse-field-Ising-chain","page":"Exact diagonalization","text":"With the interaction switched on there is no closed form for finite N at general couplings, but the matrix is still small enough to diagonalize directly:\n\nH = OpSum(vcat(\n    [Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites],\n    [Op(2 .* ops.sx, i) for i in 1:nsites],\n))\n\nM = hamiltonian_matrix(H, spec, nsites)\nM ≈ M'          # Hermitian, as it must be\n\nminimum(real(eigvals(Hermitian(M))))","title":"The transverse-field Ising chain"},{"category":"section","location":"examples/exact_diagonalization/#Checking-the-sparsity","page":"Exact diagonalization","text":"max_conn_size bounds the number of non-zeros per column without looking at a single configuration, which is what a sparse assembly wants to preallocate by:\n\ncompiled = compile(H)\nres = connected_padded(compiled, basis(dof_object(spec), nsites).states)\n\nmax_conn_size(compiled), maximum(res.counts)\n\nFor a lattice Hamiltonian whose off-diagonal terms are single hops or flips, the bound is tight — one slot for the diagonal plus one per off-diagonal term.","title":"Checking the sparsity"},{"category":"section","location":"examples/exact_diagonalization/#Working-in-a-symmetry-sector","page":"Exact diagonalization","text":"Blocking by symmetry gives smaller matrices whose spectra together reproduce the full one. See Symmetry sectors for the folding rules; the assembly is unchanged apart from indexing into the reduced basis:\n\nperm = [mod1(i + 1, nsites) for i in 1:nsites]\ndofo = dof_object(spec)\n\nspectrum = Float64[]\nfor k in 0:(nsites-1)\n    b = basis(dofo, nsites, sym(Translational(k, perm), dofo))\n    isempty(b.states) && continue\n\n    index = Dict(s => i for (i, s) in pairs(b.states))\n    res = connected_padded(H, b.states, b)\n    block = zeros(ComplexF64, length(b.states), length(b.states))\n    for n in eachindex(b.states), j in 1:res.counts[n]\n        block[index[res.configs[j, n]], n] += res.mels[j, n]\n    end\n    append!(spectrum, real(eigvals(Hermitian(block))))\nend\n\nsort(spectrum) ≈ sort(real(eigvals(Hermitian(M))))","title":"Working in a symmetry sector"},{"category":"section","location":"api/compile/#Compiling-operators","page":"Compiling operators","text":"compile turns an operator into a CompiledOperator — or, given a symmetry-reduced basis, a CompiledSector — hoisting every configuration-independent decision out of the hot loop. max_conn_size is the bound that connected_padded! buffers are sized by.","title":"Compiling operators"},{"category":"section","location":"api/compile/#Index","page":"Compiling operators","text":"Pages = [\"compile.md\"]","title":"Index"},{"category":"type","location":"api/compile/#ConnectedConfigs.CompiledOperator","page":"Compiling operators","text":"CompiledOperator{T}\n\nAn operator flattened into the form the kernel consumes, with everything that does not depend on the configuration hoisted out: Jordan–Wigner strings resolved, sums distributed over products, same-site factors multiplied out, matrix elements promoted to the single concrete element type T, and each local matrix column-compressed.\n\nBuild one with compile and hand it to connected_padded or connected instead of the operator itself. Doing so is worth it whenever the same operator is used more than once, which in variational Monte Carlo is always.\n\nTerms are split by whether they can change the configuration at all. Diagonal terms — σᶻσᶻ, n_i n_j, an uncancelled Jordan–Wigner tail — are the majority in a typical lattice Hamiltonian and are evaluated by a branch-free loop that never touches a state.\n\n\n\n\n\n","title":"ConnectedConfigs.CompiledOperator"},{"category":"type","location":"api/compile/#ConnectedConfigs.CompiledSector","page":"Compiling operators","text":"CompiledSector{T,C,L,N}\n\nA CompiledOperator together with the symmetry-reduced basis it is to be evaluated in: the state-to-index lookup and the orbit norms, both built once.\n\nMatrix elements are complex here even when the operator is real, because a symmetry representative carries the character of the operation that reached it.\n\n\n\n\n\n","title":"ConnectedConfigs.CompiledSector"},{"category":"type","location":"api/compile/#ConnectedConfigs.CompiledTerm","page":"Compiling operators","text":"CompiledTerm{T}\n\nOne product of single-site factors, ready to run.\n\nfactors act on pairwise distinct positions — same-site factors were multiplied together at compile time — so they commute and the kernel may apply them in any order. max_branch bounds how many configurations the term can produce from one input.\n\n\n\n\n\n","title":"ConnectedConfigs.CompiledTerm"},{"category":"type","location":"api/compile/#ConnectedConfigs.LocalFactor","page":"Compiling operators","text":"LocalFactor{T}\n\nOne single-site factor of a compiled term, stored column-compressed.\n\nColumn d (zero-based, the incoming local digit) occupies colptr[d+1]:colptr[d+2]-1 of outs and vals. The kernel therefore visits only the non-zero entries of the one column it needs, instead of scanning the whole matrix for each input configuration.\n\n\n\n\n\n","title":"ConnectedConfigs.LocalFactor"},{"category":"method","location":"api/compile/#ConnectedConfigs._branching-Tuple{ConnectedConfigs.LocalFactor}","page":"Compiling operators","text":"How many outputs a factor can produce from one input: its widest column.\n\n\n\n\n\n","title":"ConnectedConfigs._branching"},{"category":"method","location":"api/compile/#ConnectedConfigs._is_diagonal-Tuple{ConnectedConfigs.LocalFactor}","page":"Compiling operators","text":"Whether every non-zero entry of a compressed factor sits on the diagonal.\n\n\n\n\n\n","title":"ConnectedConfigs._is_diagonal"},{"category":"method","location":"api/compile/#ConnectedConfigs._merge_factors-Union{Tuple{T}, Tuple{Any, Type{T}}} where T","page":"Compiling operators","text":"Multiply out a term's same-site factors and drop the identities, returning nothing if the term is identically zero.\n\nFactors on distinct sites commute — Jordan–Wigner strings are already explicit by the time expand_terms returns — so gathering same-site factors is sound as long as their relative order within the product is preserved, which it is.\n\n\n\n\n\n","title":"ConnectedConfigs._merge_factors"},{"category":"function","location":"api/compile/#ConnectedConfigs.compile","page":"Compiling operators","text":"compile(operator) -> CompiledOperator\ncompile(operator, T::Type) -> CompiledOperator{T}\ncompile(operator, basis) -> CompiledSector\n\nFlatten operator once into the form the kernel runs on, so that the per-sample work contains no operator-tree traversal, no Jordan–Wigner expansion, and no dynamic dispatch.\n\ncompile is the entire reason this package is fast. It is also the only place the operator backend is consulted — see expand_terms — so an operator type from any library works here as long as it implements that interface.\n\nPassing a basis compiles the symmetry-reduced path instead, caching the state-to-index lookup that would otherwise be rebuilt over the whole basis on every call.\n\nExample\n\ncompiled = compile(H)\nfor step in 1:1000\n    res = connected_padded(compiled, states)   # no setup cost per step\nend\n\n\n\n\n\n","title":"ConnectedConfigs.compile"},{"category":"method","location":"api/compile/#ConnectedConfigs.max_conn_size-Tuple{CompiledOperator}","page":"Compiling operators","text":"max_conn_size(compiled) -> Int\n\nUpper bound on the number of connected configurations any single configuration can have under compiled, known without looking at a single sample.\n\nThis is what connected_padded! buffers must be sized by. The bound counts one slot for the diagonal plus the widest branching each off-diagonal term can produce, so it is exact for the usual lattice Hamiltonian, where every off-diagonal term is a single hop or flip.\n\n\n\n\n\n","title":"ConnectedConfigs.max_conn_size"},{"category":"section","location":"api/interface/#Backend-interface","page":"Backend interface","text":"The generic functions the compiler and the kernel talk to, and nothing else. Adding methods to them is how a basis or operator library other than SymBasis and OperatorAlgebra plugs in — see Backends in the manual for what each contract requires.","title":"Backend interface"},{"category":"section","location":"api/interface/#Index","page":"Backend interface","text":"Pages = [\"interface.md\"]","title":"Index"},{"category":"function","location":"api/interface/#ConnectedConfigs.amplitude_type","page":"Backend interface","text":"amplitude_type(operator) -> Type\n\nElement type the matrix elements of operator should be accumulated in.\n\nMust be wide enough to hold any product of operator's matrix entries. The default promotes to at least Float64 so that integer operator matrices — PAULI_X and friends are Int — do not truncate the square roots a bosonic ladder operator introduces.\n\n\n\n\n\n","title":"ConnectedConfigs.amplitude_type"},{"category":"function","location":"api/interface/#ConnectedConfigs.expand_terms","page":"Backend interface","text":"expand_terms(operator) -> Vector{<:AbstractVector{<:Pair}}\n\nFlatten operator into a plain sum of products of single-site factors.\n\nThe return value is one entry per term; each term is a vector of position => matrix pairs listing that term's factors in matrix-product order (the leftmost factor of A * B * C first). position is an Integer digit position — site i is digit i, counting from the least significant — and matrix is a square AbstractMatrix in mat[out, in] convention with any scalar coefficient already folded in. An empty factor list means the identity.\n\nThis is called once, by compile, and never on the hot path. An implementation is therefore free to do arbitrary work here — the default one resolves Jordan–Wigner strings for fermionic sites, which is the expensive part of the whole pipeline and precisely what compiling exists to hoist out of the inner loop.\n\nImplementing a backend\n\nDistinct terms are summed and factors within a term are multiplied, so an implementation must distribute sums nested inside products rather than leaving them for the caller.\n\nSee also amplitude_type, compile.\n\n\n\n\n\n","title":"ConnectedConfigs.expand_terms"},{"category":"function","location":"api/interface/#ConnectedConfigs.fold_state","page":"Backend interface","text":"fold_state(state, basis) -> (representative, phase)\n\nMap state onto the representative of its symmetry orbit in basis, together with the phase picked up by the symmetry operation that gets there.\n\nOnly used by the symmetry-reduced path. See also state_lookup, orbit_norms.\n\n\n\n\n\n","title":"ConnectedConfigs.fold_state"},{"category":"function","location":"api/interface/#ConnectedConfigs.lookup_index","page":"Backend interface","text":"lookup_index(lookup, state) -> Int\n\nPosition of state in the basis lookup was built from, or 0 when it is absent — which is how a configuration outside the symmetry sector is detected and dropped.\n\n\n\n\n\n","title":"ConnectedConfigs.lookup_index"},{"category":"function","location":"api/interface/#ConnectedConfigs.orbit_norms","page":"Backend interface","text":"orbit_norms(basis) -> AbstractVector\n\nOrbit norms of basis, indexed as lookup_index indexes it. Matrix elements between representatives are rescaled by sqrt(orbit_norms[m] / orbit_norms[n]).\n\n\n\n\n\n","title":"ConnectedConfigs.orbit_norms"},{"category":"function","location":"api/interface/#ConnectedConfigs.read_digit","page":"Backend interface","text":"read_digit(state, position) -> Int\n\nThe digit of the packed state at position, as a zero-based local index: digit d means local value local_values(spec)[d + 1].\n\nCalled in the innermost loop of the kernel, so an implementation should be allocation-free and inlineable.\n\n\n\n\n\n","title":"ConnectedConfigs.read_digit"},{"category":"function","location":"api/interface/#ConnectedConfigs.state_lookup","page":"Backend interface","text":"state_lookup(basis) -> lookup\n\nBuild, once, whatever structure lookup_index needs to find a state's position in basis. The default is a Dict from state to index.\n\nHoisting this out of the kernel is the entire point: the pre-compilation version of this package rebuilt an equivalent dictionary over the whole basis on every single call.\n\n\n\n\n\n","title":"ConnectedConfigs.state_lookup"},{"category":"function","location":"api/interface/#ConnectedConfigs.write_digit","page":"Backend interface","text":"write_digit(state, position, digit) -> state\n\nA copy of the packed state with position set to the zero-based digit.\n\nPacked states are immutable values, so this returns a new one rather than mutating. Called in the innermost loop of the kernel, so an implementation should be allocation-free and inlineable.\n\n\n\n\n\n","title":"ConnectedConfigs.write_digit"},{"category":"section","location":"manual/local_operators/#Local-operators-and-configurations","page":"Local operators","text":"Two conventions govern how a packed configuration is read, and both are easy to get silently wrong. local_operators exists to remove the second one as a source of error.","title":"Local operators and configurations"},{"category":"section","location":"manual/local_operators/#Digit-position-i-is-lattice-site-i","page":"Local operators","text":"Positions count from the least significant digit, which is what SymBasis.read and eachdigit use. An operator's site identifier is used directly as that position, so sites must be integers 1:nsites.\n\nwarning: OperatorAlgebra numbers digits the other way\nOperatorAlgebra's sparse and apply build their Kronecker products with site 1 as the most significant digit. The two matrices are therefore related by a permutation of the basis, not equal. Chain models that are symmetric under reversing the site order hide the difference completely, so a comparison that happens to pass proves less than it looks like it does — compare eigenvalues, traces and norms, or translate the index explicitly.","title":"Digit position i is lattice site i"},{"category":"section","location":"manual/local_operators/#Digit-d-means-local-value-local_values(spec)[d-1]","page":"Local operators","text":"For a spin these are the magnetic quantum numbers (-s dots +s); for a boson the occupation numbers 0:max_occupancy.\n\nusing ConnectedConfigs, SymBasis\n\nlocal_values(Spin(1 // 2)), local_values(Boson(3))\n\nThis is not the convention behind OperatorAlgebra's PAULI_Z, which puts +1 on the first basis state where SymBasis puts spin down. Building a Hamiltonian from those constants and evaluating it on SymBasis states flips the sign of every S^z — invisible in an unbiased model, and glaring the moment a longitudinal field is switched on.\n\nlocal_operators derives its matrices from the specification itself, so they agree with the digit ordering by construction:\n\nops = local_operators(Spin(1 // 2))\nops.sz\n\nNote the sign: S^z is diag(-1/2, +1/2), the negative of PAULI_Z / 2.\n\nThe constants are also 2×2 only, so they cannot describe a boson with max_occupancy > 1 or a spin above 1/2 at all:\n\nlocal_operators(Boson(3)).n","title":"Digit d means local value local_values(spec)[d + 1]"},{"category":"section","location":"manual/local_operators/#What-each-specification-returns","page":"Local operators","text":"Specification Fields\nSpin sx, sy, sz, sp (S^+), sm (S^-), id\nBoson a, adag, n, id\n\nMatrices follow the usual mat[out, in] convention, so Op(ops.adag, 1) * Op(ops.a, 2) hops a particle from site 2 to site 1.","title":"What each specification returns"},{"category":"section","location":"manual/local_operators/#Packing-and-unpacking","page":"Local operators","text":"configurations turns packed integers into physical local values, and packed turns them back. The degree-of-freedom axis comes first throughout, because Julia is column-major and that puts each configuration in contiguous memory — the layout a batched forward pass wants.\n\nspec, nsites = Boson(3), 4\ns = packed(spec, [0, 2, 1, 3])\nconfigurations(spec, s, nsites)\n\npacked rejects a value that is not one of the specification's local values, which catches the classic mistake of writing spins as 0, 1 rather than -1//2, 1//2:\n\ntry\n    packed(Spin(1 // 2), [0, 1, 0, 1])\ncatch err\n    err\nend","title":"Packing and unpacking"},{"category":"section","location":"api/connected/#The-kernel","page":"The kernel","text":"The kernel itself: connected_padded for a batch, connected_padded! for a batch into buffers you own, and connected for a single configuration. See Connected configurations in the manual for the padding and ordering conventions.","title":"The kernel"},{"category":"section","location":"api/connected/#Index","page":"The kernel","text":"Pages = [\"connected.md\"]","title":"Index"},{"category":"type","location":"api/connected/#ConnectedConfigs.Scratch","page":"The kernel","text":"Ping-pong buffers for one term's intermediate (configuration, amplitude) pairs.\n\nA term is applied one factor at a time, each factor reading the current pairs and writing the next ones, so two buffers are enough no matter how many factors there are.\n\n\n\n\n\n","title":"ConnectedConfigs.Scratch"},{"category":"method","location":"api/connected/#ConnectedConfigs._accumulate_folded!-Union{Tuple{T}, Tuple{S}, Tuple{AbstractMatrix{S}, AbstractMatrix{T}, Integer, Int64, CompiledSector{T, C} where C<:CompiledOperator{T}, Int64, S, Any}} where {S, T}","page":"The kernel","text":"Fold one connected configuration back onto its orbit representative and add it to column b, returning the updated number of entries.\n\nFolding maps many configurations onto one representative, so entries must be summed. The search for an existing entry is linear because a column holds a handful of them, where scanning beats hashing outright.\n\n\n\n\n\n","title":"ConnectedConfigs._accumulate_folded!"},{"category":"method","location":"api/connected/#ConnectedConfigs._diagonal_value-Union{Tuple{T}, Tuple{ConnectedConfigs.CompiledTerm{T}, Any}} where T","page":"The kernel","text":"Value a purely diagonal term takes on one configuration.\n\nEvery factor is diagonal, so nothing branches and no configuration is ever written: the term contributes one number, the product of the diagonal entries the configuration selects.\n\n\n\n\n\n","title":"ConnectedConfigs._diagonal_value"},{"category":"method","location":"api/connected/#ConnectedConfigs._fill_column!-Union{Tuple{T}, Tuple{S}, Tuple{AbstractMatrix{S}, AbstractMatrix{T}, Integer, CompiledOperator{T}, S, ConnectedConfigs.Scratch{S, T}}} where {S, T}","page":"The kernel","text":"Write every connected configuration of state into column b, returning how many slots were used. Slots above the returned count are left untouched; padding is a separate step.\n\n\n\n\n\n","title":"ConnectedConfigs._fill_column!"},{"category":"method","location":"api/connected/#ConnectedConfigs._pad_column!-Union{Tuple{T}, Tuple{S}, Tuple{AbstractMatrix{S}, AbstractMatrix{T}, Integer, Integer, Integer, S}} where {S, T}","page":"The kernel","text":"Fill slots from:to of column b with the inert padding: the sample, and a zero mel.\n\n\n\n\n\n","title":"ConnectedConfigs._pad_column!"},{"category":"method","location":"api/connected/#ConnectedConfigs._reshape_result-Union{Tuple{T}, Tuple{S}, Tuple{Matrix{S}, Matrix{T}, Vector{Int64}, Integer, NTuple{N, Int64} where N}} where {S, T}","page":"The kernel","text":"Trim the connection axis to what the batch actually used, and restore the batch shape.\n\nThe buffers were allocated at the operator's compile-time bound, which for a lattice Hamiltonian is usually exactly what the batch reaches — so the common case is a reshape with no copy at all, and only a batch that falls short of the bound pays for one.\n\n\n\n\n\n","title":"ConnectedConfigs._reshape_result"},{"category":"method","location":"api/connected/#ConnectedConfigs._run_term-Union{Tuple{T}, Tuple{S}, Tuple{ConnectedConfigs.CompiledTerm{T}, S, ConnectedConfigs.Scratch{S, T}}} where {S, T}","page":"The kernel","text":"Apply one term to a single configuration, returning (n, states, vals) — the number of (configuration, amplitude) pairs produced and the scratch buffers holding them.\n\nWhich of the two buffers ends up holding the result depends on how many factors the term has, so it is returned rather than assumed.\n\n\n\n\n\n","title":"ConnectedConfigs._run_term"},{"category":"function","location":"api/connected/#ConnectedConfigs.connected","page":"The kernel","text":"connected(operator, state) -> Dict\n\nEvery configuration connected to state, mapped to its matrix element langle s vert hatO vert s rangle.\n\nContributions from different terms reaching the same configuration are summed, and entries that cancel to exactly zero are dropped.\n\noperator may be a CompiledOperator, which skips the per-call setup entirely — the right choice when the same operator is queried repeatedly, as a Hamiltonian-driven Metropolis rule does twice per step.\n\nUse connected_padded for a whole batch: it returns arrays rather than a dictionary and is what a local energy should be built on.\n\n\n\n\n\n","title":"ConnectedConfigs.connected"},{"category":"function","location":"api/connected/#ConnectedConfigs.connected_padded","page":"The kernel","text":"connected_padded(operator, states) -> (; configs, mels, counts)\n\nConnected configurations and matrix elements for a batch of packed configurations — the counterpart of NetKet's get_conn_padded, and the kernel a variational Monte Carlo local energy is built from.\n\nstates may be an array of any shape. configs and mels gain one leading axis for the connections and keep the batch shape otherwise, so a vector of B states gives (max_conn, B) and an (A, B) matrix gives (max_conn, A, B). counts has the shape of states. NetKet puts the connection axis last; here it comes first, because Julia is column-major and this is what keeps one sample's connections contiguous.\n\noperator may be a CompiledOperator from compile, which skips the flattening work; passing the operator itself compiles it on every call.\n\nReturns\n\nconfigs: (max_conn, size(states)...) connected configurations.\nmels: (max_conn, size(states)...) matrix elements.\ncounts: how many entries per sample are real rather than padding.\n\nPadding\n\nDifferent configurations have different numbers of connections, so the leading axis is padded to a common length. Padded slots repeat the sample itself and carry a matrix element of zero, following NetKet. That choice matters twice over: it keeps mels a concrete numeric array rather than a Union{T,Missing} one, and it makes the local energy\n\nE_loc(s) = sum(mels[:, b] .* exp.(logψ.(configs[:, b]) .- logψ(s)))\n\ncorrect with no masking at all, because a zero matrix element contributes nothing — and a repeated sample is a configuration the wavefunction can safely be evaluated on.\n\nOrdering, and repeated configurations\n\nEntries appear in term order: the diagonal first when it is non-zero, then each off-diagonal term's contribution. Two terms reaching the same configuration produce two entries rather than one summed entry, which is what NetKet does and what keeps the kernel free of any per-sample hash table. Every consumer sums over the connection axis, so the result is unchanged; only the row count differs. Use connected when you want the summed, deduplicated matrix elements.\n\n\n\n\n\n","title":"ConnectedConfigs.connected_padded"},{"category":"method","location":"api/connected/#ConnectedConfigs.connected_padded!-Union{Tuple{T}, Tuple{S}, Tuple{AbstractArray{S}, AbstractArray{T}, AbstractArray{Int64}, CompiledOperator{T}, AbstractArray{S}}} where {S, T}","page":"The kernel","text":"connected_padded!(configs, mels, counts, compiled, states) -> (; configs, mels, counts)\n\nIn-place connected_padded, writing into caller-owned buffers.\n\nconfigs and mels must have max_conn_size(compiled) rows and the batch shape of states otherwise; counts must have the shape of states. Unlike the allocating form, the leading axis is not trimmed to the batch's actual maximum — the buffers keep their full height, with every slot above a sample's count padded inert — so the same buffers can be reused across calls whose connection counts differ.\n\nThis is the form to use inside an optimization loop, where the batch shape is fixed and allocating a fresh pair of arrays per step is pure overhead. The per-sample work allocates nothing; what remains is one small scratch buffer whose size is set by the operator's branching and not by the batch, so the cost per step stops growing with the batch size.\n\n\n\n\n\n","title":"ConnectedConfigs.connected_padded!"},{"category":"method","location":"api/connected/#ConnectedConfigs.connected_padded-Tuple{Any, AbstractArray, Any}","page":"The kernel","text":"connected_padded(operator, states, basis) -> (; configs, mels, counts)\n\nAs above, but for configurations living in a symmetry-reduced basis.\n\nEach connected configuration is mapped back to the representative of its symmetry orbit, and its matrix element is rescaled by the character of the symmetry operation together with the ratio of orbit norms — the standard factor sqrt(norm[m] / norm[n]). Configurations whose representative is absent from basis fall outside the sector and are dropped.\n\nContributions landing on the same representative are summed here, unlike the unreduced path: distinct configurations of one orbit are the same basis state, so leaving them separate would not merely be redundant, it would misreport how many basis states the sector connects.\n\nMatrix elements are complex even for a real operator, because the character need not be.\n\nPass compile(operator, basis) instead of operator to hoist the state-to-index lookup out of the call.\n\n\n\n\n\n","title":"ConnectedConfigs.connected_padded"},{"category":"section","location":"manual/backends/#Backends","page":"Backends","text":"SymBasis and OperatorAlgebra are this package's defaults, not its requirement. Everything the compiler and the kernel need from the outside world goes through a handful of generic functions, and nothing else. Another library plugs in by adding methods to them — ordinary multiple dispatch, with no package extension to load and no capability flag to set.\n\nThere are two independent seams. Implementing one does not oblige you to touch the other.","title":"Backends"},{"category":"section","location":"manual/backends/#The-operator-seam","page":"Backends","text":"Two functions, both consulted once, by compile, and never on the hot path.\n\nFunction Contract\nexpand_terms Flatten the operator into a plain sum of products of single-site factors\namplitude_type The element type matrix elements should accumulate in\n\nexpand_terms returns one entry per term; each term is a vector of position => matrix pairs listing that term's factors in matrix-product order, with any scalar coefficient already folded into a matrix. Because it runs only at compile time, an implementation is free to do arbitrary work — the default one resolves Jordan–Wigner strings for fermionic sites, which is the expensive part of the whole pipeline and exactly what compiling exists to hoist out.\n\nThe one obligation that is easy to miss: sums nested inside products must be distributed here. (A + B) * C is two terms, and the kernel only ever sees terms.\n\nA complete operator backend is therefore about ten lines:\n\nusing ConnectedConfigs, SymBasis\n\nstruct ToySum\n    terms::Vector{Vector{Pair{Int,Matrix{Float64}}}}\nend\n\nConnectedConfigs.expand_terms(op::ToySum) = op.terms\nConnectedConfigs.amplitude_type(::ToySum) = Float64\nnothing # hide\n\nThat is enough to run the whole kernel:\n\nspec, nsites = Spin(1 // 2), 4\nops = local_operators(spec)\nσx, σz = Matrix(2 .* ops.sx), Matrix(2 .* ops.sz)\n\ntoy = ToySum([\n    [[i => σz, mod1(i + 1, nsites) => σz] for i in 1:nsites]...,\n    [[i => σx] for i in 1:nsites]...,\n])\n\nstates = basis(dof_object(spec), nsites).states\nres = connected_padded(toy, states)\nres.mels[:, 1]\n\nAn empty factor list is the identity, which is how a term that cancels to a scalar is expressed.","title":"The operator seam"},{"category":"section","location":"manual/backends/#The-state-seam","page":"Backends","text":"Function Contract\nread_digit The zero-based local digit at a position\nwrite_digit A copy of the configuration with one digit replaced\n\nThese two are on the hot path — they run in the innermost loop — so an implementation should be allocation-free and inlineable. Packed states are treated as immutable values, so write_digit returns a new one rather than mutating.","title":"The state seam"},{"category":"section","location":"manual/backends/#The-symmetry-seam","page":"Backends","text":"Only needed if you want the symmetry-reduced path with a basis type of your own.\n\nFunction Contract\nfold_state Map a configuration onto its orbit representative, with the phase\nstate_lookup Build, once, whatever finds a state's index\nlookup_index A state's index, or 0 when it is absent\norbit_norms The orbit norms, indexed as lookup_index indexes them\n\nlookup_index has a default for any AbstractDict, so a backend whose state_lookup returns a dictionary implements three functions, not four. Hoisting state_lookup out of the kernel is the entire point of its existence: the pre-compilation version of this package rebuilt an equivalent dictionary over the whole basis on every single call.","title":"The symmetry seam"},{"category":"section","location":"manual/backends/#Where-the-defaults-live","page":"Backends","text":"src/backends/symbasis.jl and src/backends/operatoralgebra.jl, deliberately separate from the kernel so the seam is visible in the file layout rather than only in the documentation. The compiler and the kernel consume nothing but the functions above, which is what makes them backend-agnostic by construction rather than by good intentions — and the test suite runs a toy operator backend, defined without any OperatorAlgebra type, to keep it that way.","title":"Where the defaults live"},{"category":"section","location":"manual/connected_states/#Connected-configurations","page":"Connected configurations","text":"Given an operator hatH and a configuration vert s rangle, the connected configurations are those vert s rangle for which langle s vert hatH vert s rangle is non-zero. Everything in this package is built to produce them for a whole batch at once, because that is what a variational Monte Carlo local energy needs:\n\nE_mathrmloc(s) = sum_s langle s vert hatH vert s rangle \n                      fracpsi(s)psi(s)","title":"Connected configurations"},{"category":"section","location":"manual/connected_states/#Compile-first","page":"Connected configurations","text":"compile flattens an operator once into the form the kernel runs on. It resolves Jordan–Wigner strings, distributes sums nested inside products, multiplies out factors acting on the same site, promotes every matrix element to one concrete element type, and column-compresses each local matrix so the kernel visits only the entries it needs.\n\nusing ConnectedConfigs, OperatorAlgebra, SymBasis\n\nspec, nsites = Spin(1 // 2), 6\nops = local_operators(spec)\n\nH = OpSum(vcat(\n    [Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites],\n    [0.7 * Op(2 .* ops.sx, i) for i in 1:nsites],\n))\n\ncompiled = compile(H)\n\nThe result reports how the terms split. Terms that cannot change a configuration at all — sigma^z sigma^z, n_i n_j, an uncancelled Jordan–Wigner tail — are separated out and evaluated by a branch-free loop that never touches a state, which in a typical lattice Hamiltonian is most of them.\n\nPassing the operator itself to connected_padded works too; it just compiles on every call. Over a large batch that is amortized, but over single-configuration queries it dominates the runtime, so hoist it whenever the operator outlives the call.","title":"Compile first"},{"category":"section","location":"manual/connected_states/#The-batched-call","page":"Connected configurations","text":"states = basis(dof_object(spec), nsites).states\nres = connected_padded(compiled, states)\n\nsize(res.configs), size(res.mels), size(res.counts)\n\nconfigs holds the connected configurations as packed integers.\nmels holds the matrix elements.\ncounts says how many entries of each sample are real rather than padding.\n\nThe connection axis comes first. NetKet puts it last, but Julia is column-major, and this is the ordering that keeps one sample's connections contiguous in memory — which is what the inner loop of a local energy walks.","title":"The batched call"},{"category":"section","location":"manual/connected_states/#Batches-of-any-shape","page":"Connected configurations","text":"states may be an array with any leading dimensions. The connection axis is prepended and the batch shape is preserved, so a (steps, chains) block of Monte Carlo samples goes in directly:\n\nblock = reshape(states[1:24], 4, 6)\nr = connected_padded(compiled, block)\n\nsize(r.configs), size(r.counts)","title":"Batches of any shape"},{"category":"section","location":"manual/connected_states/#Padding","page":"Connected configurations","text":"Different configurations have different numbers of connections, so the connection axis is padded to a common length. Padded slots carry a matrix element of zero and repeat the sample itself.\n\nBoth halves of that convention earn their place. A zero matrix element makes the local energy correct with no masking at all:\n\nE_loc = sum(res.mels[:, b] .* exp.(logψ.(res.configs[:, b]) .- logψ(s)))\n\nand repeating the sample means every padded row is still a valid configuration, so the wavefunction can be evaluated on the whole array without special-casing. Marking padding missing instead would turn mels into a Union{T,Missing} array and put a branch in the innermost loop.\n\nb = argmin(res.counts)\nres.counts[b], res.mels[(res.counts[b]+1):end, b]","title":"Padding"},{"category":"section","location":"manual/connected_states/#Repeated-configurations-are-not-merged","page":"Connected configurations","text":"Two terms reaching the same configuration produce two rows rather than one summed row. This follows NetKet, and it is what keeps the kernel free of a per-sample hash table.\n\nEvery consumer sums over the connection axis, so the result is unchanged; only the row count differs. When you want the summed, deduplicated matrix elements — to build a matrix column, or to count how many distinct configurations an operator reaches — use connected, which returns a dictionary:\n\nd = connected(compiled, first(states))\nlength(d), sum(abs, values(d))\n\nEntries are ordered by term: the diagonal first when it is non-zero, then each off-diagonal term's contribution. That ordering is deterministic, which hash iteration order is not.","title":"Repeated configurations are not merged"},{"category":"section","location":"manual/connected_states/#Reusing-buffers","page":"Connected configurations","text":"connected_padded! writes into arrays you own, so an optimization loop stops allocating a fresh output every step. The buffers are sized by max_conn_size, a bound known from the operator alone without looking at a single sample:\n\nheight = max_conn_size(compiled)\nconfigs = Matrix{eltype(states)}(undef, height, length(states))\nmels = Matrix{Float64}(undef, height, length(states))\ncounts = Vector{Int}(undef, length(states))\n\nconnected_padded!(configs, mels, counts, compiled, states)\nheight, maximum(counts)\n\nUnlike the allocating form, the connection axis is not trimmed to the batch's actual maximum: the buffers keep their full height, with every slot above a sample's count padded inert. That is what lets the same buffers serve calls whose connection counts differ.","title":"Reusing buffers"},{"category":"section","location":"manual/connected_states/#Unpacking-for-a-neural-network","page":"Connected configurations","text":"Packed integers are the right representation for the kernel — compact, hashable, cheap to permute. A network wants an explicit numeric array, and configurations is the boundary between the two. The degree-of-freedom axis comes first, so each configuration is contiguous:\n\nsize(configurations(spec, res.configs, nsites))","title":"Unpacking for a neural network"},{"category":"section","location":"manual/symmetry_sectors/#Symmetry-sectors","page":"Symmetry sectors","text":"A symmetry-reduced basis keeps one representative per symmetry orbit instead of every configuration. Matrix elements between representatives are not the raw ones: a connected configuration generally lands somewhere in the middle of an orbit, and has to be folded back onto its representative, picking up the character of the operation that got there and a ratio of orbit norms.\n\nPass the basis and connected_padded does all of that:\n\nusing ConnectedConfigs, OperatorAlgebra, SymBasis\n\nspec, nsites = Spin(1 // 2), 8\ndofo = dof_object(spec)\nops = local_operators(spec)\n\nH = OpSum(vcat(\n    [Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites],\n    [Op(2 .* ops.sx, i) for i in 1:nsites],\n))\n\nperm = [mod1(i + 1, nsites) for i in 1:nsites]         # translate by one site\nb = basis(dofo, nsites, sym(Translational(0, perm), dofo))\n\nres = connected_padded(H, b.states, b)\nlength(b.states), size(res.configs)\n\nThe folded matrix element is\n\nlangle m vert hatH vert n rangle =\n    chi  sqrtfracmathcalN_mmathcalN_n \n    langle s vert hatH vert s rangle\n\nwhere chi is the character of the symmetry operation mapping s onto representative m, and mathcalN are the orbit norms. Configurations whose representative is absent from the basis fall outside the sector and are dropped.\n\nnote: Matrix elements are complex here\nEven for a real operator. The character need not be real — a momentum sector's is e^i k — so mels comes back complex on this path where the unreduced one stays real.","title":"Symmetry sectors"},{"category":"section","location":"manual/symmetry_sectors/#Representatives-are-merged,-unlike-the-unreduced-path","page":"Symmetry sectors","text":"The unreduced kernel deliberately leaves two terms reaching the same configuration as two rows. The symmetry path does the opposite and sums them, because here it is not a matter of convenience: distinct configurations of one orbit are the same basis state, so leaving them separate would misreport how many basis states the sector connects to. Entries that cancel to exactly zero — which happens often between orbit members — are dropped.","title":"Representatives are merged, unlike the unreduced path"},{"category":"section","location":"manual/symmetry_sectors/#Compiling-a-sector","page":"Symmetry sectors","text":"compile accepts a basis, and caches the state-to-index lookup along with the flattened operator. Without it, that lookup is rebuilt over the entire basis on every call:\n\nsector = compile(H, b)\nres2 = connected_padded(sector, b.states)\n\nres2.configs == res.configs, res2.mels == res.mels","title":"Compiling a sector"},{"category":"section","location":"manual/symmetry_sectors/#Samples-must-be-representatives","page":"Symmetry sectors","text":"Only states that are in the basis can be folded, so a sample that is not one is an error rather than a silently dropped row:\n\noutsider = first(basis(dofo, nsites).states)\noutsider in b.states","title":"Samples must be representatives"},{"category":"section","location":"manual/symmetry_sectors/#Checking-the-result","page":"Symmetry sectors","text":"Getting the character or the norm factors wrong yields a matrix that is the right size, sparse, and Hermitian, and has the wrong spectrum. The invariant worth testing is that the sector spectra partition the full one: concatenate the eigenvalues of every sector and they must reproduce, with multiplicity, the eigenvalues of the unreduced Hamiltonian. The test suite checks exactly that for momentum sectors, magnetization sectors, and their combination.","title":"Checking the result"},{"category":"section","location":"#ConnectedConfigs.jl","page":"Home","text":"The batched connected-configurations kernel for quantum lattice models.\n\nVariational Monte Carlo spends nearly all its time answering one question, several thousand times per optimization step: given a configuration vert s rangle and a Hamiltonian hatH, which configurations vert s rangle does hatH connect it to, and with what matrix element langle s vert hatH vert s rangle? This package answers it for a whole batch at once, returning padded arrays rather than a dictionary per sample — the Julia counterpart of NetKet's get_conn_padded.\n\nIts only dependencies are SymBasis.jl and OperatorAlgebra.jl, and even those are replaceable: the kernel reaches them through a small documented interface, described in Backends.","title":"ConnectedConfigs.jl"},{"category":"section","location":"#Key-features","page":"Home","text":"Compiled operators — compile hoists every configuration-independent decision out of the hot loop: Jordan–Wigner strings are resolved, sums are distributed over products, same-site factors are multiplied out, and every local matrix is column-compressed. What is left runs with no hash tables and no per-sample allocation.\nBatches of any shape — connected_padded takes an array of configurations with any leading dimensions and prepends the connection axis, so a (steps, chains) block of Monte Carlo samples needs no reshaping on the way in or out.\nPadding you do not have to mask — unused slots carry a zero matrix element and repeat the sample itself, so a local energy is a plain sum over the connection axis.\nAn in-place form — connected_padded! writes into caller-owned buffers sized by max_conn_size, so an optimization loop stops reallocating its output every step.\nSymmetry sectors — pass a symmetry-reduced basis and matrix elements come back folded onto orbit representatives, rescaled by the character and the orbit norms.\nSwappable backends — SymBasis and OperatorAlgebra are the defaults, not the requirement.\nAlmost no dependencies — no autodiff, no Lux, no GPU stack, so the kernel stays usable by any Monte Carlo or exact-diagonalization code. The test suite asserts it.","title":"Key features"},{"category":"section","location":"#Installation","page":"Home","text":"Requirements: Julia 1.11 or later.\n\nTo install the latest stable version, use the Julia package manager. Either use the Julia REPL package mode (by pressing ]):\n\npkg> add ConnectedConfigs\n\nor open the Julia REPL and run the following command:\n\njulia> import Pkg; Pkg.add(\"ConnectedConfigs\")","title":"Installation"},{"category":"section","location":"#Quick-example","page":"Home","text":"Build a transverse-field Ising chain and compile it once:\n\nusing ConnectedConfigs, OperatorAlgebra, SymBasis\n\nspec, nsites = Spin(1 // 2), 4\nops = local_operators(spec)          # matrices in SymBasis's digit ordering\n\nH = OpSum(vcat(\n    [Op(2 .* ops.sz, i) * Op(2 .* ops.sz, mod1(i + 1, nsites)) for i in 1:nsites],\n    [Op(2 .* ops.sx, i) for i in 1:nsites],\n))\n\ncompiled = compile(H)\n\nAsk what a batch of configurations connects to. The diagonal comes first, then one spin flip per site:\n\nstates = basis(dof_object(spec), nsites).states\nres = connected_padded(compiled, states)\n\nsize(res.configs), res.mels[:, 1]\n\nThe batch axis follows the input, so a block of Monte Carlo samples goes straight in:\n\nblock = reshape(states, 4, 4)\nr = connected_padded(compiled, block)\n\nsize(r.configs), size(r.counts)\n\nAnd packed configurations turn into the numeric array a neural network wants, with the degree-of-freedom axis first:\n\nsize(configurations(spec, res.configs, nsites))","title":"Quick example"},{"category":"section","location":"#Why-compiling-matters","page":"Home","text":"Every call that receives a bare operator has to flatten it first. Over a batch that cost is amortized; over the two single-configuration queries a Hamiltonian-driven Metropolis rule makes per step, it is the entire runtime. Compiling once, outside the loop, is the difference:\n\nCase Bare operator Compiled\n16-site Ising, batch of 1024 318 µs 278 µs\n10⁴ single-configuration queries 484 ms 5.8 ms","title":"Why compiling matters"},{"category":"section","location":"#Scope","page":"Home","text":"Building operators — algebra, normal ordering, fermionic signs, sparse and dense conversion — belongs upstream in OperatorAlgebra.jl, and this package deliberately duplicates none of it. What it adds is the one thing an operator library has no reason to provide: evaluation over a batch of samples rather than over a state vector.\n\nSampling lives in NQSSamplers.jl and wavefunctions in NQSCore.jl; neither is a dependency, and neither needs to be for this package to be useful on its own.","title":"Scope"},{"category":"section","location":"#Part-of-a-larger-ecosystem","page":"Home","text":"ConnectedConfigs.jl is developed in the NeuralQuantumStates.jl monorepo, and is usable entirely on its own — it has no idea the neural-network stack exists, and its test suite asserts as much.","title":"Part of a larger ecosystem"},{"category":"section","location":"#Important-notice","page":"Home","text":"This project is still under active development. While it includes an extensive test suite and is developed with high scientific rigor, you should always benchmark your own code. Please report any issues you encounter via the GitHub issue tracker.","title":"Important notice"},{"category":"section","location":"#Module","page":"Home","text":"What ?ConnectedConfigs shows at the REPL:","title":"Module"},{"category":"module","location":"#ConnectedConfigs.ConnectedConfigs","page":"Home","text":"ConnectedConfigs\n\nThe batched local-energy kernel: given an operator Ĥ and a batch of basis configurations |s⟩, produce every connected configuration |s′⟩ together with its matrix element ⟨s′|Ĥ|s⟩.\n\nThis is the Julia counterpart of NetKet's operator.get_conn_padded, and it is the one piece of the operator layer that OperatorAlgebra.jl does not already provide: its apply consumes a vector or a Dict of states, not a batch of samples. Everything else — building Ĥ, normal ordering, fermionic signs, sparse and dense conversion — belongs upstream in OperatorAlgebra, and this package deliberately does not duplicate any of it.\n\nThe kernel is the hot loop of variational Monte Carlo: it runs once per sample per optimization step. It is kept in its own package, depending only on SymBasis and OperatorAlgebra, so that it carries no Lux, autodiff, or GPU dependency and stays usable by any VMC code — neural or otherwise. Even those two dependencies are replaceable: the kernel reaches them only through the generic functions in interface.jl, and another library plugs in by adding methods.\n\nEntry points\n\ncompile — flatten an operator once, ahead of the loop; the reason this is fast\nconnected_padded — the batched kernel, with and without a symmetry-reduced basis\nconnected_padded! — the same, into buffers the caller owns\nconnected — a single configuration, as a Dict\nconfigurations / packed — convert between packed BaseInt states and arrays of physical local values\nlocal_operators — single-site matrices in SymBasis's digit ordering\n\nConventions\n\nThree, all easy to get silently wrong:\n\nDigit position i is lattice site i, counting from the least significant digit, which is what SymBasis.read/eachdigit use. An Op's site identifier is used directly as that position, so sites must be integers 1:nsites.\nDigit d means local value local_values(spec)[d+1] — (-s, …, +s) for a spin, 0:max_occupancy for a boson. This is not the convention behind OperatorAlgebra's PAULI_Z, which puts +1 on the first basis state; see local_operators.\nTwo terms reaching the same configuration produce two entries, not one summed entry, in the padded output. Consumers sum over the connection axis, so the result is unchanged; only the row count differs. connected gives the summed, deduplicated form.\n\nwarning: OperatorAlgebra numbers digits the other way\nConvention 1 is SymBasis's. OperatorAlgebra's sparse and apply build their Kronecker products with site 1 as the most significant digit, so their matrix and this package's are related by a permutation of the basis rather than being equal. Chain models symmetric under reversing the site order hide the difference completely — which makes a comparison that happens to pass weaker evidence than it looks.\n\nExample\n\nusing ConnectedConfigs, OperatorAlgebra, SymBasis\n\nspec, nsites = Spin(1 // 2), 4\nops = local_operators(spec)\nH = OpSum([Op(ops.sz, i) * Op(ops.sz, mod1(i + 1, nsites)) for i in 1:nsites])\n\ncompiled = compile(H)                    # once, outside the loop\nstates = basis(dof_object(spec), nsites).states\nres = connected_padded(compiled, states)\nres.mels        # (max_connections, batch)\n\n\n\n\n\n","title":"ConnectedConfigs.ConnectedConfigs"},{"category":"section","location":"api/configurations/#Configurations-and-local-operators","page":"Configurations and local operators","text":"Converting between packed integers and physical local values, and the single-site matrices that agree with SymBasis's digit ordering. See Local operators and configurations in the manual for the two conventions these exist to keep straight.","title":"Configurations and local operators"},{"category":"section","location":"api/configurations/#Index","page":"Configurations and local operators","text":"Pages = [\"configurations.md\"]","title":"Index"},{"category":"method","location":"api/configurations/#ConnectedConfigs._digit_of-Tuple{Any, Any}","page":"Configurations and local operators","text":"_digit_of(spec, value) -> Union{Int,Nothing}\n\nZero-based local digit holding value, or nothing when spec has no such local state.\n\nThe generic fallback searches local_values, which is what any new specification type gets for free. Spin and Boson skip the search: their local values are an arithmetic sequence, so the digit is a subtraction away, and packed sits on the boundary every batch of network inputs crosses.\n\n\n\n\n\n","title":"ConnectedConfigs._digit_of"},{"category":"function","location":"api/configurations/#ConnectedConfigs.configurations","page":"Configurations and local operators","text":"configurations(spec, states, nsites) -> Array\n\nUnpack BaseInt configurations into an array of physical local values — magnetic quantum numbers for a spin, occupation numbers for a boson — as taken from spec.\n\nPacked integers are the right representation for the operator kernel: compact, hashable, and cheap to permute. A neural network wants the opposite, an explicit numeric vector per configuration, and this is the boundary between the two.\n\nThe degree-of-freedom axis comes first: a vector of M states becomes (nsites, M), and an (M, batch) matrix as returned by connected_padded becomes (nsites, M, batch). Julia is column-major, so this puts each configuration in contiguous memory, which is the layout a batched forward pass wants.\n\nExample\n\nspec = Boson(3)\nres = connected_padded(H, states)\nx = configurations(spec, res.configs, nsites)   # (nsites, max_conn, batch)\n\n\n\n\n\n","title":"ConnectedConfigs.configurations"},{"category":"method","location":"api/configurations/#ConnectedConfigs.packed-Tuple{Any, AbstractVector}","page":"Configurations and local operators","text":"packed(spec, configuration) -> BaseInt\n\nInverse of configurations for a single configuration: pack a vector of physical local values back into a BaseInt.\n\nThrows an ArgumentError if a value is not one of spec's local values, which catches the common mistake of handing over a configuration built with the wrong convention — spins as 0, 1 rather than -1//2, 1//2, say.\n\n\n\n\n\n","title":"ConnectedConfigs.packed"},{"category":"function","location":"api/configurations/#ConnectedConfigs.local_dimension","page":"Configurations and local operators","text":"local_dimension(spec) -> Int\n\nNumber of local states of spec, i.e. the base of the packed integer representation.\n\n\n\n\n\n","title":"ConnectedConfigs.local_dimension"},{"category":"function","location":"api/configurations/#ConnectedConfigs.local_operators","page":"Configurations and local operators","text":"local_operators(spec) -> NamedTuple\n\nSingle-site operator matrices for a SymBasis degree-of-freedom specification, in the digit ordering SymBasis actually uses.\n\nWhy this exists rather than OperatorAlgebra's constants\n\nOperatorAlgebra ships PAULI_X, PAULI_Z, RAISE, and friends, but they are 2×2 only, so they cannot describe a boson with max_occupancy > 1 or a spin above 1/2.\n\nMore importantly, they use the opposite spin convention. PAULI_Z = [1 0; 0 -1] maps the first basis state to +1, i.e. digit 0 is spin up. SymBasis orders local states as dofo.ldof = (-s, …, +s), so digit 0 is spin down. Building a Hamiltonian from PAULI_Z and evaluating it on SymBasis states therefore flips the sign of every S^z — which is invisible in the ground-state energy of an unbiased model and glaring the moment a longitudinal field is switched on.\n\nThe operators returned here are constructed directly from spec, so they agree with the digit ordering by construction. Matrices follow the usual convention mat[out, in].\n\nReturned fields\n\nSpin: sx, sy, sz, sp (S^+), sm (S^-), id\nBoson: a, adag, n, id\n\nExample\n\nops = local_operators(Boson(3))\nops.n                       # diag(0, 1, 2, 3)\nOp(ops.adag, 1) * Op(ops.a, 2)   # hopping from site 2 to site 1\n\n\n\n\n\n","title":"ConnectedConfigs.local_operators"},{"category":"function","location":"api/configurations/#ConnectedConfigs.local_values","page":"Configurations and local operators","text":"local_values(spec) -> AbstractVector\n\nThe physical value of each local state, indexed by digit + 1 — the same ordering as dof_object(spec).ldof. For a spin these are the magnetic quantum numbers -s:s; for a boson the occupation numbers 0:max_occupancy.\n\nA range rather than a Vector, because this is called once per unpacking of a batch and there is nothing to gain from materializing it.\n\n\n\n\n\n","title":"ConnectedConfigs.local_values"}]
}
